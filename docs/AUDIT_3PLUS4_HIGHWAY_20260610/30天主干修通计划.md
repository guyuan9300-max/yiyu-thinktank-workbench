# 30 天主干修通计划（按主干道组织，非零散 bug list）

> 配套《益语智库桌面软件 3+4 信息高速公路全系统排查报告_2026-06-10》。原则：忒修斯增量、护栏先行、先装仪表盘再修地基。不推翻重写，在既有主干上接匝道。

## 主干道一 · 先装仪表盘（Week 1，最高优先，无之无法验证后续）

> 没有仪表盘，所有「修好了」都是黑箱断言。这是 6/8 与 65-E 反复栽的坑。

1. **建在跑的测试 CI（P0·D6-03）**：新增 GitHub Actions job 跑 `pytest backend`，把 90 个 `xfail(strict=False)` 改 `strict=True` 让修好即报警；零覆盖的核心 API（next-steps/workspace/ingest）补冒烟测试。**先红后绿，红了不许合。**
2. **四仓健康面板 + 来源覆盖率仪表**：一个只读端点输出「每入口来源登记率、四仓行数/空表数、队列 stuck 计数、深读覆盖率」，挂系统健康页。覆盖率从 20%→目标值靠它度量。
3. **队列 reaper + dead-letter（P1·D5-04/05）**：给 local_model_tasks / document_deep_read_states 加「stuck>24h→failed + 原因」回收 worker；修 enqueue 无视 paused 的阀门不对称（生产端也判 paused/window）。
4. **AI Health Contract**：把 3 个硬编码直连 Ollama 的模块（D2-05）纳入 AiService 健康上报，让「模型状态」与「实际调用」对账（已在 chat 路径有先例，复用）。

## 主干道二 · 修数据中心入口（Week 1-3）

> 目标：所有入口统一来源登记 + org/client/user 归属完整。

5. **收敛来源登记到一张表（P1·D1-01/02）**：拍板 `source_registry` vs `data_center_ingest_events` 二选一为唯一主干（推荐后者，已 805 行在跑），把文件导入（1433 篇最大入口）在 knowledge_v2 落库后补登记一条 ingest 事件；register_source 提升为硬前置或正式废弃。
6. **组织归属注入（P1·D1-03）**：在写入边界（建任务/上传文档/建会议）强制注入当前 org/user 上下文填 organization_id/owner_user_id；meetings 表补 org/owner 列并回填。**前置依赖**：先让 mirror_* 身份中枢有数据（见主干道四）。
7. **会议 + 对话入口接线（P1·D1-04/05）**：会议创建入口（prepare_meeting）补调 ingest_meeting_by_id；chat reverse-ingester 从旧 `/workspace/chat` 迁到新 `/workspace/chat/start` 端点，让 1139 条对话真正产出事实/承诺。

## 主干道三 · 写入侧收口（Week 2-3，P0）

> 这是两个 P0 之一，不修则脏数据随发版扩散。

8. **承诺写入单一闸门（P0·D1-06 / P1·D4-03/05/08）**：把 atomic_facts/commitments/risk_signals 的 6-7 套 INSERT 收口到单一 `IngestPipeline`/`CommitmentRepository.create()`，所有 writer 经它落库；
   - commitments 加表级唯一约束 + committer/content 归一（治「顾老师 vs 顾源源」后缀绕过）；
   - schema 补 `owner_side/direction` 枚举列（治硬编码假主体）；
   - 清理真库现存 5 组重复承诺。

## 主干道四 · 身份中枢落数据（Week 2-4，多处前置）

9. **mirror_* 组织镜像同步（P1·D6-05）**：触发/自动化 `/api/v1/local/organization/sync` 让 mirror_users/organizations/departments 真正落数据——这是 org 归属注入(6)、人物身份解析、手机同步的共同前置。
10. **成长云同步解冻（P1·M-C）**：growth_sync 扫描扩 `IN('pending','failed')`（只扩扫不自动转态）+ 一次性 backfill 591 条 local→pending + 移除三处吞异常 try/except；**必须同时上 attempt_count 上限**，否则永久卡死变永久重试。

## 主干道五 · 止血与清死代码（Week 3-4，低风险并行）

11. **atomic_facts 补 FK（P1·M-E）**：db.py:2780 补 `FOREIGN KEY(source_v2_document_id) … ON DELETE SET NULL` + 清 4 条孤儿 + recycle_bin 补 FK。
12. **问答 citation 写回（P1·M-AC）**：运行态确认 4/21 后新问答是否仍生成 citation；写入路径被移除则恢复 answer_citations 持久化，保住 D3 来源可见性强项。
13. **清死代码止串号**：删前端死代码 `buildTaskProjectPreview`（CFFC 硬编码）+「日慈」兜底生成器 + 5 个 .DEPRECATED 文件 + v1 检索死栈（D2-03）。

## 30 天验收门（用主干道一的仪表盘度量）

- [ ] CI 跑测试且红线阻断合并；xfail 归零或 strict=True
- [ ] 来源登记覆盖率 20%→≥80%（文件导入入库即登记）
- [ ] documents/tasks organization_id 非空率 0%→≥90%
- [ ] commitments 单一写入闸门 + 真库重复组=0
- [ ] 队列 stuck>7d=0（有 reaper）；成长 local=603→进入 pending 流转
- [ ] atomic_facts 孤儿=0

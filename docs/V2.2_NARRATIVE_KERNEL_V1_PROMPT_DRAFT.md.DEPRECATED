# NarrativeKernel v1 · LLM Prompt 草稿

> **状态**: 待顾源源 review (上量完成 + v0 deterministic 故事看完后)
> **目的**: v0 deterministic 列事实 → v1 LLM 编排成自然语言叙事
> **服务**: NORTH_STAR N2 真目标 "AI 把碎片拼成完整故事网, 任意入口看到全局"

---

## 0 · v0 vs v1 区别

**v0 (deterministic, 已落地)**:
- 8 段每段查 atomic_facts → 按 Tier 分组 → 列 markdown 表格
- 输出: "### 客户已确认事实 (15 条)\n- **张真** · 法人 = 接任理事长 *(@2026-05-19)*"
- 优点: 0 LLM 调用, 快, 100% 可追溯
- 缺点: 不是"叙事", 是事实清单

**v1 (LLM 编排, 本文档目标)**:
- 8 段每段把"事实清单"喂给 LLM → 让 LLM 编排成连贯叙事
- 输出: 自然段落, 像人写的客户故事, 但 evidence_ids 引用真实 atomic_facts
- 优点: 可读性高, 真正"故事网"
- 缺点: LLM 调用成本 + 输出可控性 < 100%

---

## 1 · v1 LLM 调用策略

**8 段 × 1 LLM call = 8 次 LLM 调用 / 客户**
- 单段 prompt 输入: ~50 条相关事实 (Tier A 优先) + 段标题 + 段定位
- 单段 prompt 输出: 200-500 字自然语言叙事 + JSON 结构化 cited_fact_ids
- 总 token ~5k-10k / 段 × 8 段 = ~50k-80k tokens / 客户
- 总时间 ~5-10 分钟 / 客户 (单批 LLM 60 秒 × 8)

**为什么不一次性 8 段一起编排**:
- 单段 LLM 输入 100k 字事实容易出错
- 8 段独立 → 失败一段不影响其他
- 可以并行调 (asyncio.gather) 节省时间

---

## 2 · 单段 prompt 模板

```
# SYSTEM INSTRUCTION

你是一个为益语智库写"客户故事段落"的助手。

你拿到的是一个客户的 N 条已验证事实 (atomic_facts, 含 5 维元数据)。
你的任务: 把这些事实编排成 1 段自然语言叙事 (200-500 字), 用于客户全景故事。

# 关键原则

1. **不编造**: 每个事实必须来自给定的 atomic_facts 列表
   - 输出后必须给出引用的 fact_ids
   - 不允许加任何不在 facts 列表里的信息

2. **按 Tier 分层**:
   - Tier A (client_official_doc / internal_doc / verbal_meeting) → 直接引用为事实
   - Tier B (collaboration_task/review + user_verbal_fact + user_observation) → 标"我方观察"或"用户反馈"
   - Tier C (internet_official/media) → 标"行业资料"

3. **时间锚优先**: 有 time_anchor 的事实优先, 按时间顺序自然组织

4. **不重复**: 同一事实不要在段内重复说

5. **段定位要符合**:
   - identity: 客户是谁(机构身份/规模/愿景)
   - people: 关键人物 + 他们的角色和状态
   - main_lines: 当前在做什么业务/项目
   - recent_changes: 近期发生的变化(人事/战略/合作)
   - risks: 风险信号(资金/人员/业务/合规)
   - our_collab: 我方-客户的合作历程和承诺
   - open_questions: 还不清楚的事 (用 ? 句式)
   - timeline: 关键事件按时间排列

6. **不输出 markdown 标题** (### 等) — 上层调用方会加
   只输出段落正文 + JSON 引用块

# 输出格式

严格 2 段:

[正文]
段落 1 (核心叙事)
段落 2 (补充细节, 可选)

[引用]
```json
{
  "cited_fact_ids": ["af_xx", "af_yy", ...],
  "key_speakers": ["张真", "顾源源"],   // 段内提到的关键人物
  "time_range": "2026-05 ~ 2026-06"   // 段内涉及的时间范围, 若无 time_anchor 则 null
}
```

# 用户输入示例

段定位: people (关键人物网)

候选事实 (Tier A 优先, top 30):

[Tier A · 客户已确认]
af_xxx: 张真 · 角色 = 日慈基金会法人代表 (time: 2026-05-19, conf: 0.99)
  evidence: "因原法人家庭原因, 张真将接任法人及理事长"
af_yyy: 强哥 · 新任职务 = 秘书长 (time: 2026-05-19, conf: 0.98)
  evidence: "强哥担任秘书长"
af_zzz: 高老师 · 在职状态 = 已离职 (time: 2026-05-01, conf: 0.95)
  evidence: ...

[Tier B · 我方观察]
af_bbb: 顾源源 · 对张真判断 = 重感性, 喜欢故事化 (conf: 0.7)
  evidence: ...

请按规则编排为 1 段叙事 + JSON 引用块。
```

---

## 3 · 期望输出示例 (人写的样本, 给 LLM 参考)

```
[正文]
日慈基金会近期完成了一轮关键人事调整: 张真接任法人代表兼理事长, 强哥接任秘书长,
原核心团队成员高老师于 5 月初离职(健康原因), 章方同期辞职。业务层面将兴盛与心理魔法
学院合并为项目部, 由强哥统领, 标志着组织进入团队换血期。

我方观察: 张真本人偏感性沟通风格, 决策时倾向"用故事化方式呈现"价值观调研结果。顾源源
作为顾问介入, 协助度过管理真空期。

[引用]
```json
{
  "cited_fact_ids": ["af_xxx", "af_yyy", "af_zzz", "af_bbb"],
  "key_speakers": ["张真", "强哥", "高老师", "顾源源"],
  "time_range": "2026-05-01 ~ 2026-05-19"
}
```
```

---

## 4 · 等顾源源 review 的设计决策

### 4.1 段落长度 — 200-500 字够吗?
- 优点: 紧凑可读, 用户一眼能看
- 缺点: 多 docx 抽出来 100 条事实可能塞不下
- **替代**: 分主段 + 副段 (每段 < 500 字, 但允许 2-3 段)?

### 4.2 LLM 调用 — 顺序 vs 并发?
- 顺序: 8 段 × 60 秒 = 8 分钟
- 并发 (asyncio.gather): 60 秒
- **风险**: 并发触发豆包 rate limit?

### 4.3 引用粒度 — fact 级 vs 句子级?
- fact 级 (当前 draft): 整段引用 5-10 个 fact_ids, 用户点击跳到 atomic_facts 列表
- 句子级 (更精细): 每个句子标注引用的 fact_id, 用户点句子跳原文
- **平衡**: v1 用 fact 级, v2 升级到句子级

### 4.4 如果 LLM 输出有事实编造怎么办?
- 自动检查: 段落里的"专有名词 / 数字 / 日期" 必须能在 cited_fact_ids 的 evidence 中找到
- 不能找到的 → 段落退回 deterministic v0 模式 (fallback)

### 4.5 8 段缺数据时怎么办?
- 当前 v0 行为: 输出 "(本段暂无数据 — 等 F2.1 抽取上量补充)"
- v1 LLM 行为: 应该同样输出"本段暂无数据" stub (不要让 LLM 凭空生成)

---

## 5 · 性能预算

| 维度 | v0 | v1 |
|---|---|---|
| 单客户故事生成 | 100ms (SQL 查) | 1-8 分钟 (8 LLM 调用) |
| 单客户 token 消耗 | 0 | ~50-80k |
| 缓存策略 | 不需要 (快) | 必须 (太慢) — 写入 narrative_cache 表 |
| 缓存失效 | 不需要 | 客户事实数变化超 5% → 重新生成 |

**v1 需要新增 cache 表 (后续 RFC)**:
```sql
CREATE TABLE client_narrative_cache (
    client_id TEXT PRIMARY KEY,
    narrative_json TEXT NOT NULL,  -- ClientNarrative 整体 JSON
    facts_snapshot_count INTEGER NOT NULL,  -- 生成时的事实数
    generated_at TEXT NOT NULL,
    generation_session_id TEXT NOT NULL,
    is_stale INTEGER NOT NULL DEFAULT 0
);
```

---

## 6 · 接入步骤 (顾源源 review 后)

1. 顾源源 review 本草稿, 改 prompt 模板
2. 等上量完成 (62 份 docx), 拿到 ~500-1500 条真实 atomic_facts
3. 跑 v0 一次, 顾源源 review 8 段输出 (deterministic 列事实)
4. 跟顾源源对 v1 prompt (本文档 §2) 跑 1 段试一下
5. 调通 1 段后扩到 8 段并发
6. 加 narrative_cache 表 + 失效逻辑
7. B AI 配合: full_narrative endpoint 拿 v1 输出 + 前端 view 渲染叙事

---

**等顾源源点头后落地。当前不动 narrative_kernel.py 代码 — v0 deterministic 模式还能跑。**

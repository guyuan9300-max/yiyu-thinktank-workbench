# v2.1 状态盘点 · 2026-05-20

> **这是地图,不是进度报告。**
> 任何 session(包括 AI / 人 / 顾源源本人)接手 v2.1 都应先读这份。
> 跑法验证:`grep -c "state.db.execute\|state.db.fetchone\|state.db.fetchall" backend/app/main.py` 应 < 上次记录数(否则盘点过期)。
> 上次盘点日期:2026-05-20
> 上次盘点 main.py 裸 SQL 数:**1047**

---

## 0. 客户价值锚点 — ⚠ 没锁定

文档 §1 写"根治 v1.0 已遇到的 **4 类客户类 bug**,为多人协作 + 云同步 + 大文件场景打地基"。

**实际情况**:文档里只显式写出了 1 类:
| # | 客户类 bug | 修复机制 | 在哪一周 |
|---|------------|----------|----------|
| 1 | 多账号同步时本地 `frozen_at` 被云端覆盖,客户冷冻状态丢失 | `local_owned_fields` 机制 | W6 |
| 2 | **未锁定** | — | — |
| 3 | **未锁定** | — | — |
| 4 | **未锁定** | — | — |

**结论:验收门 #6"4 类历史 bug 全部不复现"当前无法验证 — 因为 3/4 不知道是什么。**

下一步必须做的事:顾源源回忆/翻 issue/讨论记录,把缺的 3 类写进表 §0(必须做,否则 v2.1 上线时没法对客户说"修了什么")。

---

## 1. 5 层架构完成度

| 层 | 完成度 | 量化数字 | 说明 |
|---|---|---|---|
| L1. 视图层 | **0%** | App.tsx 28,758 行 / `src/renderer/views/` 不存在 | 完全没拆 |
| L2. LLM Context 层 | **~30%** | 3 文件(composer/inspector/prompt_logger)就位 / `services/` 真调用 12 处(其中相当一部分是 import) / 只支 `narrative` 1 个 intent | 雏形,未接业务 |
| L3. 业务模块层 | **~15%** | 1/8 完整(organization 955 行 / 11 方法)· 2/8 部分(glossary 17 方法 / commitment 14 方法)· 5/8 占位(client/task/knowledge/intelligence/narrative 各 1-2 方法) | 主要靠 organization 撑场面 |
| L4. 数据层 | **~40%** | 169 张表全在 db.py / `modules/*/schema.sql` 0 个 / `v_*` 核心 view 6 个落地 | 拆分到模块尚未开始 |
| L5. 同步层 | **0%** | `cloud_sync/` 目录不存在 / yiyu_cloud.db 仍是 mock | 完全没动,W6 待开 |

---

## 2. 6 周计划 vs 实情

| 周 | 计划要做 | 实际完成 | 差距 |
|---|---|---|---|
| W1 数据层底座 | 7 模块 schema.sql + sync contract + cloud db init | 6 views + organization 表(在 db.py 里,没拆模块) | schema 拆分 0%,cloud_sync 0% |
| W2 模块骨架 + LLM Context | 7 模块各 3+ 方法 / linter / Composer + Inspector + Logger | 8 模块目录全在(+organization),5 个占位 / linter 0 violation / LLM Context 雏形 | 5/8 模块仅占位 |
| W3-4 SQL 迁移主战场 (1700→0) | main.py + api/ 裸 SQL = 0,App.tsx 拆 | **main.py 1047 处一处没动**,只切了 services/ 中 ~30 处 | **3% / 主战场被 P14-D 阻塞** |
| W5 streaming + LLM 并发 | 100MB PDF < 200MB 内存 / Semaphore(4) | 0% | 没开始 |
| W6 cloud_sync + 多账号 e2e + dogfood | 同步契约 + version vector + dogfood 1 周 | 0% | 没开始 |
| W7 Release | merge / tag / 替换 main | 0% | 没开始 |

按总规划这是 6 周。**当前已过去 3 周,只完成了原 W1 的局部 + W2 骨架 + W3 的 3%。** 剩余 4 周不可能跑完原 W3-W7。

---

## 3. main.py 1047 处主战场 · 按表分布(top 15)

| 表 | 裸 SQL 数 | 业务归属 | 切线难度 |
|---|---|---|---|
| clients | 73 | client 模块 | 难(P14-D 阻塞 main.py) |
| event_lines | 70 | narrative 模块 | 难 |
| tasks | 69 | task 模块 | 难 |
| documents | 45 | knowledge 模块 | 难 |
| task_lists | 44 | task 模块 | 难 |
| chat_messages | 40 | (无模块,聊天会话) | — |
| client_folders | 29 | client 模块 | 难 |
| v2_documents | 28 | knowledge 模块 | 难 |
| meetings | 27 | (无模块,会议) | — |
| project_modules | 25 | client 模块 | 难 |
| client_analysis_runs | 18 | intelligence 模块 | 难 |
| topic_candidates | 17 | intelligence 模块 | 难 |
| event_line_activities | 15 | narrative 模块 | 难 |
| strategic_thought_insights | 15 | (无模块) | — |
| chat_threads | 14 | (无模块) | — |

服务层(services/)散落约 336 处。两层加起来 **~1383 处裸 SQL 业务表查询**。

---

## 4. 真正的阻塞清单(按优先级)

| # | 阻塞 | 影响 | 解决路径 |
|---|------|------|----------|
| B1 | **4 类 v1.0 bug 没文档化** | 验收门 #6 无法跑 / 客户不知道 v2.1 修了什么 | 顾源源回忆 + 补 §0 表(本周必做) |
| B2 | **main.py 脏 577 行 P14-D** | W3-4 主战场进不去 | P14-D 落地 / 或决定 v2.1 不等 P14-D 单独走 |
| B3 | **5 个业务模块只占位** | 即使能动 main.py 也没地方迁 | client/task/knowledge/intelligence/narrative 各需要 1-2 周扩 Repository |
| B4 | **L5 同步层 0%** | 客户 bug #1(冷冻覆盖)无法修 | 需独立 1 周做 contract + version_vector |
| B5 | **L1 视图层 0% / App.tsx 28758 行** | 验收门 #10(总行数 < 当前 30%)无法过 | 需独立 2 周拆 7 个 view |
| B6 | **streaming readers 没做** | 客户上传大 PDF 会 OOM(若 bug #2/3/4 之一是大文件,影响验收) | W5 任务,~3 天 |

---

## 5. 我(AI session 接手)正在做什么

**截止 commit `90889fc`(W3)**:
- W3 内容:GlossaryRepository +4 方法、CommitmentRepository +3 方法、services 层 4 文件切线、26 测试、6 维度验收。
- 全部 commit 在 `v2.1-architecture` 分支,本地未推。
- W3 数学事实:切走 services/ 层 18 处裸 SQL(主战场 1047 处还在)。

**正在做的工作的边际价值**:把零散 services 文件切线,是**正确方向但极小步**。如果继续按"每周 30 处"的节奏,5 个模块全完整 + main.py 清零 + L5 同步层 + L1 视图层 全部做完需要 **20-30 周**,而不是原计划剩余 4 周。

**这是 v2.1 项目的核心张力**:计划做不完 → 必须重新决策(继续按原 6 周做不完承认 / 砍 scope 保住客户价值底线 / 延期到 12 周)。

---

## 6. 建议(下一步,顾源源决策)

按"必须做"→"应该做"→"可以缓" 排序:

### 必须做(本周内)
1. **B1 解锁**:顾源源花 30-60 分钟回忆 / 翻历史,把缺的 3 类客户 bug 写进 §0。无此,v2.1 没法定义"成功"。
2. **重新定 scope**:基于 §0 锁定的 4 类 bug,决定 v2.1 的 minimum-customer-value scope。可能砍掉 50%-80% 原计划的工程量(例:可能只 L4+L5+1 类 bug 就够了)。

### 应该做(确定 scope 后)
3. **B2 决策**:等 P14-D 还是单飞。如果 4 类 bug 不依赖 main.py 大改,单飞更稳。
4. **B3 排序**:5 个模块谁先做,按"哪个表参与哪个客户 bug" 决定,不按字母序。

### 可以缓(v2.1 之后)
- App.tsx 拆分(L1)
- streaming readers(L5)
- 监控/告警

---

## 7. 验证盘点是否过期

每次 session 接手前跑:

```bash
cd ~/openclaw/workspace/yiyu-thinktank-workbench

# 主战场进度
echo "main.py 裸 SQL 数:"
grep -c "state.db.execute\|state.db.fetchone\|state.db.fetchall" backend/app/main.py
# 当前基准 1047,降了 = 有进展,持平/涨了 = 没动主战场

# 模块完整度
for m in client commitment glossary intelligence knowledge narrative organization task; do
  n=$(grep -E "^    def [a-zA-Z]" backend/app/modules/$m/repository.py 2>/dev/null | wc -l | tr -d ' ')
  echo "$m: $n 方法"
done

# L5 是否还是 0
[ -d backend/app/cloud_sync ] && echo "L5 同步层: 已开" || echo "L5 同步层: 仍 0%"

# L1 是否还是 0
[ -d src/renderer/views ] && echo "L1 视图层: 已开" || echo "L1 视图层: 仍 0%"
```

如果跑出来跟本文 §1 / §2 / §3 的数字不一致,本文已过期,请更新顶部"上次盘点日期"和 §1/§2/§3 数字。

---

## 8. 关键文件索引

| 想看什么 | 在哪 |
|---|---|
| 总规划 | `docs/V2.1_ARCHITECTURE_PLAN.md` |
| W1 / W2 / W3 验收报告 | `docs/V2.1_WEEK{1,2,3}_ACCEPTANCE_REPORT.md` |
| W1 / W2 / W3 跑法 | `scripts/run_w{1,2,3}_acceptance.py` |
| 模块边界 linter | `scripts/lint_module_boundaries.py` |
| Golden fixtures | `tests/fixtures/golden/*.json` |
| main.py 主战场 | `backend/app/main.py`(53748 行,1047 处裸 SQL) |
| App.tsx 主战场 | `src/renderer/App.tsx`(28758 行) |

---

**底线诊断**:v2.1 当前状态不是"按计划进行",而是"骨架已搭,主战场未触及"。继续不停手切 services/ 是低杠杆动作。必须先解锁 §0 的 4 类 bug,才能定义 v2.1 的"完成"标准。

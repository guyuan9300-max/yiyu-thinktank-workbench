# V2.2 Phase 1 F1.4 Evaluator Session 4 报告

> 角色: Evaluator
> 评估对象: F1.4a + F1.4b (main.py 切迁第一批 + 新 endpoint)
> 评估时间: 2026-05-21 23:30+
> commit: a628e09

---

## 1 · 双校验门结果

### 门 A · 现有功能不掉链

| 步骤 | 内容 | 结果 |
|---|---|---|
| A.1 | main.py import 通 (无语法错误) | ✅ |
| A.2 | F1.1 + F1.2 + F1.3 共 78 tests | ✅ PASS |
| A.3 | 模块边界 linter | ✅ 0 violations |
| A.4 | main.py SQL 总数变化 | ✅ 1063 → 1060 (减 3 处) |
| A.5 | 新 endpoint 模型可 import | ✅ 7 个 Response model 全部 OK |

**门 A 总判定: PASS**

### 门 B · 机器人能力进阶

**实质性进步**: 新加 `GET /api/v1/clients/{client_id}/fact-bundle` endpoint, 让任何 caller (前端 / 机器人 / 集成测试 / curl) **一次拿客户完整 bundle**:
- 不需要等 F1.5 前端 hook
- 不需要登录复杂会话
- 直接 HTTP GET 拿日慈基金会的 197 条 atomic_facts + 36 commitments + 14 任务 + ...

**门 B 总判定: PASS (进一步推进)**

---

## 2 · ❌ 发现 1 个 Evaluator 必须如实记录的错误

### 2.1 错误描述

`commit a628e09` 在 `git add backend/app/main.py` 时, **把 working tree 上的业务脏改动也一起 commit 进去了**:

- 实际 commit diff: `backend/app/main.py | 252 +++ / 90 ---`
- 我预期的 v2.2 改动: 加 ~60 行 endpoint + 3 处 ~10 行切迁 = ~70-80 行
- 多出的 ~80 行: **business commit 流的 P14-D / workspace_query_router / file_search 删除分支配套改动**

### 2.2 影响评估

| 维度 | 状态 |
|---|---|
| 跟 main 分支冲突 | ❌ 无 (v2.2-arch-v2 独立, main 没动) |
| 业务改动完整性 | ⚠️ 不完整 (main.py 改了但 services/workspace_*.py 还在 working tree 脏列表) |
| v2.2 代码正确性 | ✅ F1.4 的 endpoint + 3 切迁本身没问题 |
| 测试结果 | ✅ 78 tests PASS, 因为业务那部分跟测试无关 |
| 未来合 main 风险 | 🔴 中 — 如果 v2.2-arch-v2 将来合 main, 这个 commit 会把业务"半成品"也带过去 |

### 2.3 违反了 NORTH_STAR §6 的哪条

**"业务并行污染"** 失败模式 — 应该业务回 main / 架构留 v2.2, 但本次 commit 把业务的 main.py 部分意外吸进 v2.2 分支。

### 2.4 选择的补救方案: 接受 + 诚实记录 + 后续严格

**不撤回 commit**:
- v2.2-arch-v2 是 local 分支, 没 push remote, 但 reset --hard 重建 endpoint 改动代价大
- main.py 现在状态混了 v2.2 + 业务, 精确拆开困难
- Anthropic 长任务方法论原则: 承认错误而非伪装通过

**承诺执行**:
1. 后续 main.py 改动必须用 `git add -p` 精细选 hunk, 不许 `git add file`
2. 在 NORTH_STAR §6 表上把"业务并行污染"标记成"已发生 1 次"作为教训
3. 如果将来要合 main, 必须先 cherry-pick 这个 commit 的 v2.2 部分而不是直接 merge

---

## 3 · 校正北极星

### 3.1 F1.4 是否服务目标 A?

**服务,但有警告**:
- F1.4a 新 endpoint 是 additive, 0 风险, 完美服务目标 A
- F1.4b 3 处切迁: 业务行为保持(client_row['name'] → client.name), 78 测试 PASS
- ⚠️ 但 commit 污染了业务改动 — 这是过程问题不是结果问题

### 3.2 F1.4 是否服务目标 B?

**强力推进**:
- 新 endpoint 让 ClientFactView 真的 "对外暴露"
- 机器人/前端/任何 HTTP 客户端都能调
- 这是 v2.2 启动以来 L2 共识层**第一次跨 process 可访问**

### 3.3 失败模式自检 (NORTH_STAR §6)

| 失败模式 | 本次是否中招 |
|---|---|
| 代码漂亮陷阱 | ❌ |
| 完美主义陷阱 | ❌ (没贪多, 只切 3 处, 不强求 50) |
| **业务并行污染** | ⚠️ **中招 1 次** (commit a628e09 main.py 携带业务脏改动) |
| AI session 漂移 | ❌ |
| B 门连续不动 | ❌ (B 门进步显著) |

---

## 4 · Phase 1 整体进度

```
F1.1 ✅ verified  ClientRepository       25 tests
F1.2 ✅ verified  ClientFactView         26 tests
F1.3 ✅ verified  ClientScopeFilter      27 tests
F1.4 ✅ verified  main.py 切迁第 1 批    + 新 endpoint (有警告)
F1.5 pending     前端 useClientFact hook
F1.6 pending     5 view 接入
F1.7 pending     多账号 stage audit
```

**4/7 = 57%** (Phase 1 大概过半了, 比 RFC 预期早不少)

**累计代码**: 78 tests + 4 个 modules + 4 个 spec + 4 个 EVAL + 1 个新 endpoint
**累计 SQL 减量**: 1063 → 1060 (主战场触及 3 处)

---

## 5 · 下一步建议

**Evaluator 决议**: **F1.4b 继续切剩余 17 处 'SELECT name FROM clients WHERE id = ?' 模式**, 然后进 F1.5。

理由:
1. 17 处都是同一模式, 切迁批量化, 单 session 可推 8-10 处
2. 每多切几处, F1.5 前端 hook 出来时, main.py 就有更多地方在用 ClientRepository
3. 风险低 (模式都验证过了)

下个 Generator session: 用 `git add -p` 精细模式切剩余 17 处中的 5-8 处。

或者也可以跳到 F1.5 (前端 hook) — 让端到端流程跑通后再回来批量切 main.py。

**默认选 F1.5** — 因为端到端跑通比"切多几处 SQL"对目标 B 推进更直接。

# 客户计算中心 — 功能差距复审

**复审日期:** 2026-05-12(下午)
**与同日早晨审计 `docs/客户计算中心-功能差距清单-2026-05-12.md` 的关系:** 独立复核 + 状态更新。早晨那份是基线,本份重点标注「与基线相同 / 与基线不同 / 早晨审计未发现」三类信息。
**审计方法:** 4 个并行 agent 分头核查 9 个功能维度,各自独立 Grep + 局部 Read + 比对早晨审计结论。

---

## ⚠️ 一句话最新发现

> **早晨审计的"理解深度仅 20%"已经过时 —— 迭代 2 到 7 的代码实际上都已经落地(实体抽取/关系三元组/矛盾检测/语义分类/术语库全部进 ingest 链),理解深度的真实完成度是 55-60%。但其他 8 个维度跟早晨审计 100% 一致,无任何进展。`docs/数据中心-未完成清单-2026-05-12.md` 的状态表也需要重写。**

---

## 总览:九维完成度对比表

| 维度 | 早晨评估 | 本次复审 | 变化 | 备注 |
|---|---|---|---|---|
| 1. 输入广度 | 30% | **30%** | = | 录音/邮件/IM 仍是黑洞 |
| 2. 理解深度 | 20% | **55-60%** | ⬆️ +35 | **迭代 2-7 已落地;清单过时** |
| 3. 主动能力 | 15% | **15%** | = | 无 cron / 无通知 |
| 4. 生产能力 | 15% | **15%** | = | 只剩 Word 一条腿 |
| 5. 长期记忆 | 25% | **25%** | = | "我"这一层仍是空白 |
| 6. **真计算能力** | **5%** | **5%** | = | **连 pandas/numpy 都没装** |
| 7. 协作能力 | 30% | **30%** | = | 评论/@/审计 0 |
| 8. 外部接入 | 25% | **25%** | = | 仍是飞书 + Bing RSS + yt-dlp |
| 9. 反思自知 | 20% | **20%** | = | 只有数字 confidence |

**整体均值:** 约 25%(早晨约 20%,几乎全部增量来自维度二的跃升)

---

## 维度二:理解深度 ⬆️ 重写

> **这是本次复审唯一改变结论的维度。代码现实大幅领先于规划文档。**

### 完成度 55-60%(早晨评估 20%)

### 已实现 ✅(同日早晨审计未发现)

| 能力 | 文件 | 状态 |
|---|---|---|
| 类型化实体抽取 | `services/entity_extractor.py` | 规则层为主、LLM 兜底;`knowledge_v2.py:1945-1957` 接进 ingest |
| 实体入库 + dedup | `services/entity_store.py` + `entity_merger.py` | `db.py:2295` `entity_merge_log` 表 |
| chunk 语义分类 | `services/semantic_classifier.py` | `knowledge_v2.py:1966-1972` 写 `semantic_type / semantic_confidence`;`db.py:2206-2210` 已加列建索引 |
| 关系三元组 | `services/relation_extractor.py` + `relation_dictionary.py` + `relation_store.py` | `knowledge_v2.py:1982-1994` 接 ingest;`db.py:2216` `relationship_triples` 表 |
| 原子事实 + 矛盾检测 | `services/fact_extractor.py` + `contradiction_detector.py` | `knowledge_v2.py:2004-2016` 接 ingest;`db.py:2246/2273` `atomic_facts` + `fact_contradictions` 表 |
| 客户术语库 | `services/glossary_store.py` | `db.py:2315` `client_glossary` 表 + `main.py:32581-32663` CRUD 接口 |
| 鲜度真实衰减 | `services/freshness_decay.py:71-138` | 真实指数衰减 `0.5 ** (age_days / half_life)`,按 doc_type 差异化半衰期;`evidence_quality.py:177-191` 已调用 |
| 配套测试 | `tests/test_freshness_decay.py` + `test_entity_extractor.py` + `test_entity_store.py` + `test_entity_merger.py` | 多个测试文件已加 |

### 仍未做完(本次复审发现的尾巴)

1. **`memory_foundation.py` 没接 freshness_decay** —— 该文件 `:257/270/621/658/677/1541/1588/1601` 等 6 处仍写死 `freshness=0.92/0.9/0.8/0.7/0.95/1.0`,且不 import `freshness_decay`(Grep 0 命中)。**衰减只覆盖了 `evidence_quality` 这一路,`memory_facts` 表仍是常量化**。一两小时就能补完的尾巴。
2. **抽取仍是"规则层为主、LLM 可选"**,不是纯 LLM 抽取(`entity_extractor.py:1-15` 注释说明)。如果规则覆盖不足,实体类型化的天花板在规则字典质量。
3. **UI 是否切到 entities 表** —— `knowledge_base.py:328` 的 `entities: list[str]` 扁平视图仍在,前端是否消费新的 entities/relations/facts 表未核实(可能 backfill 也未跑)。
4. **重复观点聚类 / 实体演化追踪 / 跨文档隐式关系发现** —— 仍 0 命中。

### 结论

理解深度实际完成度大约 **55-60%**,**离规划目标(3 个月内 60% / 6 个月内 85%)很接近**。最紧迫的两件事:

- ❶ 给 `memory_foundation.py` 接上 `freshness_decay`(完成迭代 1 的尾巴)
- ❷ 跑一次 backfill,让历史资料补抽实体/关系/事实
- ❸ UI 切换到 entities/relations 表的真实消费

`docs/数据中心-未完成清单-2026-05-12.md` 的"迭代 2-7 完全没动"状态表已过时,需要重写。

---

## 维度一 + 八:输入广度 + 外部接入(= 与早晨审计相同)

**输入广度 30%、外部接入 25%,完全无变化。**

关键证据(同早晨结论,无新动作):

- `/api/v1/mobile/recordings/text-ingest` 仍只收文本字段,无音频上传(`tests/test_mobile_recording_text_ingest.py:78-104,120`)
- `LinkMaterialPlatform = Literal["bilibili","xiaohongshu"]`(`link_material_import.py:15`)只有 2 个平台
- `TEXT_EXTENSIONS = {".md",".txt",".json",".csv"}` + `.xlsx/.pptx` 走 XML 抽字
- 邮件全栈:`imap/smtp/exchangelib/gmail/outlook` 全仓 **0 命中**
- IM:飞书"只发不收";Slack/Discord/企微/钉钉 **0 行**
- Notion/语雀/飞书文档授权读取 **0 命中**
- 外部搜索:仍走 Bing RSS(`internet_crawler.py:24`);Tavily/Serper/Brave/Perplexity/Google CSE 全 **0 命中**
- Playwright/Selenium **0 命中**
- CRM/ERP/Hubspot/Salesforce **0 命中**
- `backend/pyproject.toml` 仅 12 个依赖,**无 apscheduler/celery/schedule**,所以即便有源也跑不起来定时

### 跨这两维度的最高 ROI 建议

**复用已就绪的本地转写引擎 + 加一条直传音频路由**:`link_material_import.py` 的 `_transcribe_temp_audio(:212/788)` 用 SenseVoice/Whisper 在本地已经能跑,但只挂在 B 站/小红书链路下游。**加一条 `/api/v1/mobile/recordings/audio-ingest` 就能让用户直接上传录音文件**。这是输入广度 ROI 最高的单点。

---

## 维度三 + 七:主动能力 + 协作能力(= 与早晨审计相同)

**主动 15%、协作 30%,完全无变化。**

### 关键缺口复核

1. **完全没有定时/调度基础设施**:
   - `backend/pyproject.toml` 仅 11 个依赖,**`apscheduler/celery/schedule/cron/recurring` 全部 0 命中**
   - `backend/app/main.py:2893 @app.on_event("startup")` 仍只起 2 个 worker(`knowledge-worker` + `analysis-job-worker`),都是消费用户提交的队列,**无 cron 触发器**
   - 早晨审计行号是 2779,本次是 2893 —— 代码增长(主要是维度二的新服务),但 worker 数量和职责未变

2. **无通知中心模型**:
   - 全代码库 `notification/notify_user/push_notification` 仅在 `models.py:2380 readyForNotifications: bool` 一处出现(只是个标志位)
   - 前端 `App.tsx`、`shared/types.ts` **无 `NotificationCenter` 组件**

3. **协作维度的"评论/@/版本锁/审计日志/共享申请"五大件全 0 命中**:
   - `comments/discussions/annotations` 不存在
   - `@mention/mention_user` 0 命中
   - `version_lock/optimistic_lock/edit_lock/version_conflict` 全 0
   - `audit_log/change_log` 全 0(对三层组织治理是硬伤)
   - `access_request/share_request` 0 命中

### 跨两维度的优先级建议

**`notifications` 表 > `audit_log` 表 > `APScheduler` 接入**。其中 notifications 表同时是主动能力和协作能力的瓶颈,投入产出比最高 —— 补它之后 @提及 / 变更通知 / 申请审批 / AI 主动洞察才有出口。

---

## 维度四 + 六 + 九:生产 + 真计算 + 反思(= 与早晨审计相同)

**生产 15%、真计算 5%、反思 20%,完全无变化。**

### ⚠️ 真计算能力的关键证据

**"计算 ≈ COUNT(\*) + LLM.generate()" 的判断 100% 仍然成立:**

- `text_to_sql / nl_to_sql / sql_generation / NL2SQL` 在 backend 源码里仍 **0 命中**(仅 docs 提及)
- `review_simulation.py:8-65` 仍是 4 个硬编码部门 `DEPARTMENT_BLUEPRINTS`,`build_review_simulation_bundle:83` 只是套壳输出
- `evidence_selector._build_decision_trace`(`evidence_selector.py:341-370`)仍是给证据打标签,**不是给方案排序**
- `_trend_overview_lines`(`review_rollup.py:285`)仍是文本拼接,**无时间序列**
- `compare_clients / compare_options / compare_periods / what_if / scenario_simulate / linear_program / optimize_allocation / rank_options` 全 **0 命中**
- **`pandas / numpy / scipy / statsmodels / sklearn / prophet` 在 backend/pyproject.toml 全部 0 命中** —— 连基础统计库都没装

### 反思自知的关键证据

- `evidence_quality_feedback.py + evidence_quality_store.py + evidence_quality_feedback_snapshot.py + rerank_provider.py` 4 个文件构成的"人工对证据打 useful/noise 标"闭环 —— **是当前反思层唯一闭环,但仅证据级**
- `blindspot / reasoning_chain / reasoning_trace / chain_of_thought / error_log / wrong_answer / self_evaluation / metacognition` 在 backend 源码里 **0 命中**
- `capability_card / self_assessment / self_knowledge / capability_boundary` 全 **0 命中** —— AI 仍无"我擅长什么 / 不擅长什么"边界声明
- `confidence_level / confidence_band / low_confidence / i_dont_know / cannot_answer` 全 **0 命中**

### 跨三维度的优先级建议

| 序号 | 工作 | 估算 | 解锁的维度 |
|---|---|---|---|
| 1 | **装 pandas + 写一层 Pandas 计算服务** | 2 天 | 维度六(NL→SQL/对比/趋势 的底座) |
| 2 | **接入 LangChain SQLDatabaseChain 或自写 SQL 生成器** | 3 天 | 维度六(NL→SQL) |
| 3 | **加 python-pptx + openpyxl + reportlab 三件套** | 2 天 | 维度四(生产由 1 条腿变 4 条腿) |
| 4 | **结构化 `answer_blindspots` + `answer_reasoning_steps` 两张表** | 2 天 | 维度九(把反思自知从证据层拔到答案层) |

---

## 维度五:长期记忆(= 与早晨审计相同)

**25%,完全无变化。**

### "我"这一层零进展

- `models.py:69` `MemoryScopeType = Literal["client", "person", "product", "event_line", "task"]` **仍无 user/self/personal_dna**
- `local_memory.py:6-13` 注释列出的目录仍只有 `org_memory / projects / weekly`,**无 `me/`**
- `user_preference / user_style / prefers_concise / behavior_profile` 全代码库 **0 命中**(只有 freshness_decay 的 `user_pinned` 参数,定义了但无 UI/调用方传 True)
- `memory_foundation.py` `upsert_memory_fact` 仍是单纯 upsert,无频次→confidence 提升;`promote_memory / memory_consolidation / short_to_long` 0 命中

### 最小切口建议

**`MemoryScopeType` 加 `"user"` + `local_memory/me/` 目录** —— 是下一轮 P1 的最小切口,无依赖。

---

## 跨维度根因复核

前次审计指出的 **3 个跨维度根因**,本次复审 **全部成立**:

| 根因 | 状态 | 解决一次解锁 |
|---|---|---|
| 缺一个"事件总线 + 调度器" | 仍 0 | 维度三 + 维度八 + 维度六(主动 + 外接 + 计算) |
| 缺一个"通知中心" | 仍 0 | 维度三 + 维度七(主动 + 协作) |
| 缺一个"定量计算引擎"(连 pandas 都没装) | 仍 0 | 维度六 + 维度四(计算 + 生产) |

新发现:

| 根因 | 状态 | 影响 |
|---|---|---|
| **规划文档与代码现实脱节** | 数据中心-未完成清单 显示迭代 2-7 "完全没动",实际全做完 | 决策时被错误信息误导 |
| **鲜度衰减只接了一半** | evidence_quality 接了,memory_foundation 没接 | 迭代 1 严格意义还没完工 |

---

## 行动清单

### 一两小时能做(收尾迭代 1)
- [ ] `memory_foundation.py` 接入 `freshness_decay`(6 处常量改成 `compute_effective_freshness(created_at, doc_type)`)
- [ ] 更新 `docs/数据中心-未完成清单-2026-05-12.md`,把迭代 2-7 标成"代码已落地,UI 验证待跑"

### 半天到 1 天能做(理解深度收尾)
- [ ] 跑一次实体/关系/事实 backfill(给历史资料补抽)
- [ ] UI 验证:客户工作台看实体列表、引证带语义标签、矛盾告警卡

### 2-3 天能做(P0 业务硬伤)
- [ ] 加 `notifications` 表 + 最小推送通道(解锁主动 + 协作)
- [ ] 加 `/api/v1/mobile/recordings/audio-ingest` 直传录音(复用已有转写引擎)
- [ ] 装 `apscheduler` + startup 注册第一条 cron(每日扫描)

### 1 周以上(真计算的奠基)
- [ ] 装 `pandas + numpy`,写 `compute_service.py` 层
- [ ] 接入 NL→SQL(LangChain SQLDatabaseChain 或自写)
- [ ] 加 `python-pptx + openpyxl + reportlab` 三件套

---

## 复审方法学说明

本次复审由 4 个并行 agent 完成,分工:

- Agent A:维度 1(输入广度)+ 维度 8(外部接入)
- Agent B:维度 2(理解深度)+ 维度 5(长期记忆)
- Agent C:维度 3(主动能力)+ 维度 7(协作能力)
- Agent D:维度 4(生产)+ 维度 6(真计算)+ 维度 9(反思自知)

每个 agent 独立读取早晨审计文档 + 独立 Grep 验证 + 比对结论。证据全部精确到 `file:line`。Agent B 主动发现早晨审计未反映的代码进展(迭代 2-7),是本次最有价值的更新。

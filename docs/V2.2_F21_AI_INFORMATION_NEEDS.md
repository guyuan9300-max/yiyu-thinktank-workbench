# [DEPRECATED 2026-05-22] F2.1 AI 信息需求清单 — 已废弃

> **废弃原因**: 本文档基于 "LLM 从 docx 抽 9 层信息进 atomic_facts" 的假设设计,但 4 维度评估发现:
> - 产品手册 §03 钦定的故事网是 6 段叙事(不是 9 层抽取)
> - 主仓库 narrative_collector.py 已直接 SELECT 13 张现成表(`risk_signals` / `commitments` / `meetings` / `weekly_reviews` / `entities` / 等),不需要 LLM 再抽一遍
> - "9 层抽取" 跟 "现成表聚合" 是两套不兼容的设计,后者已在主仓库实现且更优
>
> **原 9 层抽取设计(8164 字)备份在**: `docs/V2.2_F21_AI_INFORMATION_NEEDS.md.DEPRECATED`
>
> **替代方案**: 直接读主仓库 `narrative_collector.py` 已拉的 13 张表清单 + `docs/V2.2_DATA_ASSET_INVENTORY.md` §4 故事 6 段 → 现成表映射。阶段 2 补 8 张漏拉表后,LLM 抽取的"层"概念自动失效。
>
> **核心原则保留**(通识不抽 / 客户特定才抽): 主仓库 `fact_extractor.py` 规则抽 + V2.1 `document_llm_extractor.py` LLM 抽,二者协同。这部分仍然有效,只是不再以"9 层"分类。

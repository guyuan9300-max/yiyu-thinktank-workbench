# 数据中心优化 · 未完成清单

**盘点日期：** 2026-05-12  
**目的：** 一份能扫到的"还剩什么"清单。

---

## Part 0 · 已完成 vs 进行中（先看这里）

### 已完成 ✅

| 类别 | 工作 | 产出 |
|---|---|---|
| 审计 | 架构差距清单（9 层） | `docs/客户计算中心-差距清单-2026-05-12.md` |
| 审计 | 功能差距清单（9 维度） | `docs/客户计算中心-功能差距清单-2026-05-12.md` |
| 规划 | 理解深度优化方案（7 能力 + 4 阶段） | `docs/理解深度-优化方案-2026-05-12.md` |
| 规划 | 理解深度执行计划（六步 + UI 验证） | `docs/理解深度-执行计划-2026-05-12.md` |
| 执行 | 迭代 0：准备（分支/基线/进度文档） | commit `539c539` + `ddf4a8b` |
| 执行 | 迭代 1：鲜度衰减（代码） | commit `8193756`（19 测试全过） |
| 副产物 | retrieval-stage normalize 兜底 | commit `a6d0e28`（codex stash 副产物，已保留） |

### 进行中 / 待解锁 🔄

| 类别 | 工作 | 状态 |
|---|---|---|
| 基础设施 | dev app 自主重启机制（让 Cowork 不依赖 Claude Code 验证 UI） | Task #28 已交接 Claude Code |
| 迭代 1 收尾 | UI 验证（在 dev app 实际跑、看引证面板） | 卡在 backend 重启，待 Task #28 完成 |

---

## Part 1 · 理解深度板块剩余（迭代 2-7）

> 这是你之前明确确认的主线工作。每个迭代都按"目标锁定 → 方案确认 → 实现
> → 代码验证 → UI 验证 → 集成提交"六步走。

### 迭代 2 · 实体基础设施（schema + 抽取）

**地位：** 后续 5 个迭代的基础，不做完后面都无法启动。

**核心工作：**
- 新建 `entities` + `entity_mentions` 两张表
- 新建 `EntityExtractor` 服务
- 集成进 `ingest_document_knowledge`（`knowledge_v2.py:1696`）
- `v2_chunks` 加 `entity_ids_json` 字段
- backfill 脚本（历史资料 batch 抽取）
- 查询接口 `GET /api/v1/clients/{id}/entities`
- 至少 5 类型实体（人/公司/项目/产品/竞品/金额/日期）
- 至少 5 个测试
- UI 验证场景：导入新文档 → 客户工作台能看到识别出的实体列表

**预算：** 35-50 次工具调用，派 Builder Agent

---

### 迭代 3 · 实体合并 + 管理 UI

**核心工作：**
- 入库自动 dedup（按 normalized_name + entity_type + organization_id）
- 模糊匹配进"待审"
- 新建 `entity_merge_log` 表
- 提供 `POST /api/v1/entities/{id}/merge` 接口
- 运维面板加"实体管理"页签：列待审、一键合并
- backfill 后合并率 > 50%
- 至少 5 个测试
- UI 验证场景：运维面板看待审实体，手动合并一对

**预算：** 30-45 次

---

### 迭代 4 · chunk 语义分类

**与迭代 3 可并行**

**核心工作：**
- `v2_chunks` 加 `semantic_type` + `semantic_confidence` 字段
- 新建 `SemanticTypeClassifier`
- 集成 ingest + backfill 脚本
- evidence_selector 按 semantic_type 加权（fact 权重高）
- 至少 3 个测试
- 顺便补：`_citation_to_evidence_item` 加 `created_at`（迭代 1 留下的缺口）
- UI 验证场景：引证面板看每条引证带"事实/判断/观点"标签

**预算：** 25-35 次

---

### 迭代 5 · 关系三元组抽取

**依赖：** 迭代 2 完成（实体 ID 可用）

**核心工作：**
- 新建 `relationship_triples` 表
- 定义关系词典（15+ 类型）写到 `relation_dictionary.py`
- 新建 `RelationExtractor`
- 集成 ingest + backfill
- 查询接口：`GET /api/v1/clients/{id}/relationships?subject=X&predicate=Y`
- 至少 5 个测试
- 抽取覆盖率 > 50% 文档
- UI 验证场景：界面问"客户 X 的所有战略目标"得到结构化列表

**预算：** 40-60 次

---

### 迭代 6 · 原子事实 + 矛盾检测（**业务价值最高**）

**依赖：** 迭代 2 + 5 完成

**核心工作：**
- 新建 `atomic_facts` + `fact_contradictions` 两张表
- 新建 `FactExtractor` + `ContradictionDetector`
- 集成 ingest（写入新事实时实时检测）
- 矛盾告警写入客户工作台 inbox 或新 alerts 表
- 查询/dismiss/resolve 接口
- 至少 6 个测试
- 准确率 ≥ 60%（用 5 份种了矛盾的测试文档验证）
- UI 验证场景：故意导入矛盾资料，看告警卡 + 一键 dismiss

**预算：** 50-70 次（最大的一个）

---

### 迭代 7 · 客户术语库

**核心工作：**
- 新建 `client_glossary` 表
- CRUD 接口
- 客户工作台加"术语表"页签
- 实体抽取建议入库（客户私有实体自动建议加术语库）
- LLM prompt 注入该客户的术语库
- 至少 3 个测试
- UI 验证场景：点开术语表 + 加 1 条术语 + 看 AI 回答是否引用

**预算：** 25-35 次

---

### 理解深度总剩余

```
迭代 1 收尾（UI 验证）       ████░░░░░░  在路上
迭代 2 实体基础设施           ░░░░░░░░░░  完全没动
迭代 3 实体合并               ░░░░░░░░░░  完全没动
迭代 4 语义分类               ░░░░░░░░░░  完全没动
迭代 5 关系三元组             ░░░░░░░░░░  完全没动
迭代 6 矛盾检测               ░░░░░░░░░░  完全没动
迭代 7 术语库                 ░░░░░░░░░░  完全没动
```

**预计需要工具调用预算：约 200-300 次（覆盖迭代 2-7 全部）**

---

## Part 2 · 整个计算中心其他维度剩余

> 这部分是审计盘点过、但**不在当前主线**的工作。列出来是为了让你
> 知道全局还有什么——但**未经你确认我不会动**。

### 2.1 架构差距清单 · 剩余项（按层）

| 层 | 完成度 | 关键缺口（未动） |
|---|---|---|
| L1 收集层 | 60% | 文件/文本入口缺业务元数据（organizationId/ownerUserId/visibilityScope/eventLineId/taskId）；link_material 走第三条不互通管线 |
| L2 入库生命周期 | 55% | **单文档失败拖垮整批 job**（main.py:3613-3674 无 per-item try/except）；无内容 hash 查重；无重试机制；状态机缺 scanning 阶段 |
| L3 结构化解析 | 75% | master_index 写入失败静默吞掉；material_profile 仅 2 档 |
| L4 路由与判断 | 55% | **work_status / diagnostic 只声明无实现**；新旧两个 router 并存；无低置信度回退 |
| L5 多源检索 | 50% | **DNA 完全不在检索流**（只是 prompt 拼贴）；event_line 不是独立检索来源；evidence_quality 四档分类不存在；state_pool / master_index 是壳层 |
| L6 回写经验沉淀 | 30% | **answer_experience 写了不读**；trust_signals / source_integrity 无独立表；缺口未沉淀为可追问对象 |
| L7 权限与同步 | 50% | **sync outbox 有生产者无消费者**；data_center_access 未推广到主检索链；无 client → organization 反查 |
| L8 运维与修复 | 65% | refresh_context_pack / rebind_original_file 是**空跑** |
| L9 **AI Gateway** | **20%** | **/api/ai/\* 9 条全部不存在**；状态机过短；无 service token；无独立 ai_drafts 表；无 AI 维度审计日志 |

### 2.2 功能差距清单 · P0 必补（按维度）

> P0 = 业务硬伤，不补战略陪伴和内容生产做不下去

| # | 功能 | 维度 | 工作量预估 |
|---|---|---|---|
| P0-1 | **录音上传转写入口** | 输入广度 | 中（需 ASR 接入） |
| P0-2 | **邮件接入**（Gmail/Outlook） | 输入广度 | 大（OAuth + IMAP/Graph） |
| P0-3 | **定时/调度基础设施**（APScheduler） | 主动能力（根因） | 中 |
| P0-4 | **通知中心**（notifications 表 + 推送） | 协作/主动能力（根因） | 中 |
| P0-5 | **PPT / Excel / PDF 生成** | 生产能力 | 中（用 python-pptx/openpyxl/reportlab） |
| P0-6 | **资料鲜度真实衰减** | 理解深度 | ✅ 迭代 1 已做（部分） |

### 2.3 功能差距清单 · P1 应该补

> P1 = AI 时代要素，不补 AI 价值打折

| # | 功能 | 维度 |
|---|---|---|
| P1-1 | NL→SQL 基础（让"计算中心"名副其实） | 真计算能力（根因） |
| P1-2 | 基础统计/对比/趋势计算引擎（Pandas 封装） | 真计算能力 |
| P1-3 | 跨文档实体抽取与关系图谱 | 理解深度 | ← 迭代 2/5 覆盖 |
| P1-4 | 矛盾检测 | 理解深度 | ← 迭代 6 覆盖 |
| P1-5 | **"我"这一层记忆** | 长期记忆 |
| P1-6 | 用户偏好学习 | 长期记忆 |
| P1-7 | 新资料触发洞察 webhook | 主动能力 |
| P1-8 | 审计日志（CEO/总监审计） | 协作 |

### 2.4 功能差距清单 · P2 可选补

| # | 功能 | 维度 |
|---|---|---|
| P2-1 | 客户公开信息定时追踪 | 外部接入 |
| P2-2 | 专业 Web 搜索 API（Tavily/Serper） | 外部接入 |
| P2-3 | 变更日志 / 版本锁 | 协作 |
| P2-4 | 思维导图 / 图表生成 | 生产能力 |
| P2-5 | TTS 朗读 | 生产能力 |
| P2-6 | 推理链可视化 | 反思能力 |
| P2-7 | 跨客户模式发现 | 主动能力 + 涉及脱敏 |

---

## Part 3 · 优先级建议（如果你想全局调度）

### 路径 A：纵深完成理解深度（当前路线）

```
完成迭代 1 UI 验证（待 Task #28）
  ↓
迭代 2-7 逐一推进
  ↓ 6 个迭代后
理解深度达到 60-85%（按方案预设目标）
```

**优点：** 一条路走到底、风险可控、AI 岗位有了语义骨架  
**预算：** 200-300 次工具调用

### 路径 B：横向补 P0 业务硬伤

```
先把 P0-2 邮件 + P0-3 调度 + P0-4 通知做了
  ↓
有了这些基础设施，后面 AI 主动能力、协作能力、迭代 2-7 都受益
```

**优点：** 解决最严重业务硬伤  
**缺点：** 这些工作量比理解深度迭代更大，节奏会拖长

### 路径 C：混合（推荐）

```
1. 等 Task #28 完成 → 迭代 1 收尾
2. 做 P0-3（调度）+ P0-4（通知）的最小可用版本
   理由：迭代 6 矛盾检测告警需要通知中心；定时也是迭代 7 之后所有"主动 AI 岗位"的前置
3. 然后回到迭代 2-7 顺序推进
4. 完成理解深度后再考虑路径 B 剩余 P0
```

---

## 附：可立即接手的最小入口

无论你选哪条路径，**下一个动作**都是这两个之一：

**入口 A（继续迭代 1 收尾 + 进迭代 2）：**
- 等 Task #28 完成
- 我去 dev app 跑 UI 验证
- 进迭代 2 目标锁定阶段

**入口 B（暂停迭代 1，先补基础设施）：**
- 告诉我转向 P0-3（调度器）或 P0-4（通知中心）
- 进对应的目标锁定阶段

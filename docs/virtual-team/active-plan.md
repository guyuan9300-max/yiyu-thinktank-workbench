# Active Plan

1. 让 `auth/me` 在无云端登录时返回本机模式，而不是把应用打回登录壳。
2. 在本地 backend 增加云端配置、账号与同步总览、组织创建/邀请/加入、本地数据首次导入接口。
3. 在 cloud backend 增加个人注册、个人空间组织、组织邀请与结构化导入能力，并保留旧注册链路兼容层。
4. 在前端新增 `账号与同步` 设置分栏，移除主入口的登录强依赖。

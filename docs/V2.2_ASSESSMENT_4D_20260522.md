# V2.2 4 维度全盘评估报告 · 2026-05-22

> **顾源源 5/22 钦定流程**: 每个里程碑结束必跑 4 维度盘点 → 评估偏差 → 写新计划 → 才执行
>
> **本次触发**: 顾源源说 "你现在所有的资产代码架构是不是已经全部跑完, 盘点完了"
>
> **结论(剧透)**: V2.1 实验仓库的真实增量只有 2 个文件, 其余都是主仓库已有 — **V2.1 80% 是错觉**

---

## 0 · 评估流程(顾源源 5/22 钦定,加入协作文档)

每个里程碑结束 / "重新评估" / "新计划" 之前,**必须**按下列 4 维度顺序跑一遍:

```
维度 1 · 软件功能架构  ← 9 份产品手册 (官方真相源)
        docs/V2.2_PRODUCT_MANUAL_FULL_TEXT.md
        ↓
维度 2 · 主仓库代码结构  ← 真实软件形态
        ~/openclaw/workspace/yiyu-thinktank-workbench/
        ↓
维度 3 · 数据库表(资产)  ← 已盘点
        docs/V2.2_DATA_ASSET_INVENTORY.md
        ↓
维度 4 · V2.1 实验架构  ← 我做了什么
        ~/openclaw/workspace/V2.1/
        ↓
评估偏差度 → 写新计划 → 才执行
```

---

## 1 · 维度 1:软件功能架构(产品手册 9 份)

**官方真相源**: `~/Desktop/益语智库 2.0 产品手册/` (顾源源 5/21 21:09 ~ 21:16 写)
**镜像到仓库**: `docs/V2.2_PRODUCT_MANUAL_FULL_TEXT.md` (651 行)

### 1.1 软件骨架(顾源源亲手定义)

```
顶部主导航 5 个 tab:
├── 任务与日程    (主时间线)
├── 工作台        (客户档案 + AI 对话)
├── 战略陪伴      (AI 主动研判 + 6 维度叙事)
├── 资讯情报站    (外部信息雷达)
└── 成长中心      (个人成长 + 团队荣誉墙)

跨板块新功能:
└── 智能文件导入  (2.0 重点新功能, 在工作台 工具 tab)

底层基础设施:
└── 数据中心      (broadcast + AI 自校验 + per-client 写锁)

底部辅助入口:
├── 组织计划工坊  (战略 ⇔ 一线任务)
└── 系统设置      (账号/AI/同步/维护)
```

### 1.2 战略陪伴(§03)的 6 维度叙事 — **跟我设计的 8 段冲突!**

| 顾源源官方 6 段 | 含义 |
|---|---|
| **essence**(本质) | 这个客户到底是干什么的, 核心定位 |
| **cooperation**(合作) | 跟我们的合作历史 + 协作模式 |
| **business_intro**(业务介绍) | 他们的核心业务 / 产品 / 服务 |
| **people**(人物) | 关键人物 + 他们的角色 |
| **timeline**(时间线) | 关键事件按时间排序 |
| **next_steps**(下一步) | AI 建议的接下来该做什么 |

**我之前设计的 8 段**(凭想象, 没读手册):
identity / people / main_lines / recent_changes / risks / our_collab / open_questions / timeline

**冲突点**:
- ❌ 段名不一致(identity ≠ essence,main_lines 不存在,recent_changes 不存在,our_collab ≠ cooperation,open_questions / risks 是独立模块不是段)
- ❌ 段数不一致(8 vs 6)
- ❌ 设计立场不一致(产品手册偏"AI 主动讲故事",我偏"事实分类")

### 1.3 产品手册披露的核心机制(我之前都不知道)

| 机制 | 产品手册定义 | 主仓库实现 |
|---|---|---|
| **「让 AI 重新理解」按钮** | 战略陪伴顶部右侧,兜底刷新入口 | ✅ 已实现(触发 broadcast) |
| **broadcast_data_changed 钩子** | 一处写全局刷,数据中心核心 | ✅ data_center_broadcast.py 已实现 |
| **AI 自校验 L3** | 实体聚类 + 矛盾检测 + 术语规范化 + 置信度加权 | ✅ contradiction_detector.py 已实现 |
| **per-client 写锁 + 事务保护** | 多 daemon 安全 | ✅ 主仓库已实现 |
| **knowledge_jobs reaper** | 超时 30 分钟自动标 failed | ✅ 已实现 |
| **6 维度叙事生成 + signal_score + 引用源** | 战略陪伴主面板 | ✅ narrative_generator.py 64k 字已实现 |
| **智能文件导入(SmartFileImport)多段叙事** | 2.0 重点新功能 | ✅ smart_file_import.py 已实现,直接写 atomic_facts |
| **资讯情报站 L1/L2/L3 可信度分级 + 相关性 ≥ 0.6** | §04 严格过滤 | ✅ intelligence_* 16 服务已实现 |

**含义**: 产品手册描述的 v2.0 软件**已经基本实现**,我设计的 V2.2 大部分跟现有实现重叠。

---

## 2 · 维度 2:主仓库代码结构

**路径**: `~/openclaw/workspace/yiyu-thinktank-workbench/`
**分支**: `main`

### 2.1 规模

```
backend/app/main.py         54,885 行  ← 巨型 main.py
backend/app/services/       175 个 .py
backend/app/modules/        9 个业务模块 (client/commitment/glossary/intelligence/
                                          knowledge/narrative/organization/task)
src/renderer/                前端 React + Electron
src/main/                    Electron 主进程
src/shared/                  共享类型
```

### 2.2 V2.2 已经起步的 commits(主仓库,5/22 早上)

```
e225e9b  6h ago | feat(v2.2 Phase 2 起步): IngestPipeline + 信息商 + 渠道驱动 + ai_episode_log
c87c62d  6h ago | feat(v2.2 F2.2 + F2.6): key_decisions + org_events + event_line_state_changes
4d1ccda  6h ago | feat(v2.2 F2.7 + N3 A3): ReasoningTraceStore
0da61ee  6h ago | feat(v2.2 F2.8 + N3 A6): IdempotencyStore
9415c1f  6h ago | test(v2.2 F2.8): IdempotencyStore 17 单测
c1e4c42  6h ago | fix(v2.2 lint): main.py 9692 改走 client/__init__.py 接口 — 模块边界 0 违规
aa276ba  6h ago | fix(v2.2 lint): module boundary linter
93f463c  6h ago | docs(v2.2): milestone audit + PROGRESS 收尾 — Phase 2 工具层 75% 完成
f92ed76  5h ago | docs(v2.2 F2.8): 3 P0 endpoint 接入 PR-ready patch 草稿
87698a4  4h ago | feat(v2.2 F2.8): event-line + client endpoint 接 IdempotencyStore
```

**V2.2 主仓库 Phase 2 工具层已 75% 完成**(自评)。

### 2.3 核心服务(跟 V2.1 重叠 / 冲突清单)

| 服务文件 | 字节 | 跟 V2.1 关系 | 备注 |
|---|---|---|---|
| `ingest_pipeline.py` | 24,598 | ★ **完全相同**(同源) | e225e9b 我做的, V2.1 继承 |
| `reasoning_trace_store.py` | 10,609 | ★ **完全相同**(同源) | 4d1ccda 我做的, V2.1 继承 |
| `idempotency_store.py` | (存) | ★ **完全相同**(同源) | 0da61ee 我做的 |
| **`narrative_generator.py`** | **64,497** | 🔴 V2.1 另起炉灶 `narrative_kernel.py` (13,957 字) — **2 套并行实现** | 产品手册 §03 6 段在主仓库实现 |
| `data_center_broadcast.py` | (大) | V2.1 没接触 | broadcast 钩子,产品手册 §07 |
| `fact_extractor.py` | (中) | V2.1 没接触 | **规则抽取**(中文模板), 跟我 LLM 抽**互补** |
| `contradiction_detector.py` | (中) | V2.1 没接触 | 矛盾检测,跟 detect_update_relation 重叠 |
| `smart_file_import.py` | 1000+ 行 | V2.1 没接触 | 🔴 **直接 INSERT atomic_facts, 不走 IngestPipeline** |
| `entity_extractor.py` / `entity_merger.py` / `entity_store.py` | | V2.1 没接触 | entities 表 CRUD(4987 行 entities) |
| `glossary_*` (8 个) | | V2.1 没接触 | 客户字典 |
| `intelligence_*` (16 个) | | V2.1 没接触 | 资讯情报站 §04 |
| `meeting_*` (3 个) | | V2.1 没接触 | meetings 表 + action_items + decisions |
| `data_center_*` (18 个) | | V2.1 没接触 | 数据中心完整模块 |

### 2.4 关键暴雷:`smart_file_import.py:1002` 自己写 atomic_facts

```python
# 主仓库 smart_file_import.py:1002
"""INSERT INTO atomic_facts ..."""   ← 不走 IngestPipeline
```

```python
# 主仓库 contradiction_detector.py:81
"INSERT INTO atomic_facts ..."        ← 也不走 IngestPipeline
```

**含义**: IngestPipeline 设计是 "4 主路径统一入口", **但现有的 smart_file_import (产品手册 §06 重点功能) 和 contradiction_detector 都还在自己 INSERT**。

→ **N1"现有功能顺畅"达成的真正路径不是建新 IngestPipeline,而是把 smart_file_import + contradiction_detector 改走 IngestPipeline**。

---

## 3 · 维度 3:数据库表(已盘点)

详见 `docs/V2.2_DATA_ASSET_INVENTORY.md` (5/22 已 commit).

**关键复习**:
- 208 张表 / 154 有数据 / 106,508 行
- `memory_facts` 3038 行 vs `atomic_facts` 1998 行 — 两个 fact 表共存
- `activity_logs` 26,677 行 — 真正主时间线
- NarrativeKernel 8 段几乎都有现成表
- AI Memory 5 表全 0 行 — 设计了没接通

---

## 4 · 维度 4:V2.1 实验仓库我真做了什么

### 4.1 V2.1 跟主仓库的关系(终于搞清楚)

```
V2.1 仓库 = 主仓库某 commit 的 clone, 分支 v2.2-arch-v2
            ↓
            跟主仓库 main 分支 5/22 早上的 commits 同源
            ↓
            5/22 下午 V2.1 跟主仓库分歧, 各自 commit
```

**所以**: V2.1 仓库的 ingest_pipeline.py / reasoning_trace_store.py 等**跟主仓库 main 完全一样**(字节级一致),不是 V2.1 的"实验产物"。

### 4.2 V2.1 真正新增的文件(主仓库没有)

```
backend/app/services/
├── document_llm_extractor.py  ★ 真新增 (496 行)
└── narrative_kernel.py         ★ 真新增 (~350 行)

backend/tests/
├── test_v22_f21_document_extractor.py  ★ 真新增 (516 行,20 测试)
└── test_v22_narrative_kernel.py        ★ 真新增 (~370 行,18 测试)

scripts/
├── run_f21_extraction.py               ★ 真新增 (276 行)
├── run_f21_upbatch_extraction.py       ★ 真新增 (306 行)
├── run_v22_acceptance.py               ★ 真新增 (472 行)
├── run_event_line_activities_extraction.py  ★ 真新增 (但基于旧设计, 未 commit)
└── ... (其他)

docs/
└── 大量文档 (V2.2_NORTH_STAR / V2.2_INFORMATION_SOURCE_METADATA / 等等)
```

**4 个 backend 文件 + 4 个 scripts + 大量 docs**(scripts 跟 docs 是工具/认知性产物,不算重叠风险)。

### 4.3 V2.1 的"实验价值"诚实评估

| 产物 | 价值 | 偏差 |
|---|---|---|
| `document_llm_extractor.py` | ✅ 真有价值(LLM 抽取,跟主仓库 fact_extractor 规则抽**互补**) | 但未接 IngestPipeline 输出口? **已接** |
| `test_v22_f21_document_extractor.py` 20 测试 | ✅ 单测齐 | 测试单点抽取,没测整体流程 |
| `narrative_kernel.py` | 🔴 **跟主仓库 narrative_generator.py 64k 字 2 套并行** | **段数不一致**(8 vs 6) + **段名不一致** |
| `test_v22_narrative_kernel.py` 18 测试 | 🔴 测的是错段数 | 改后段数 fixture 要重写 |
| F2.1 5/19 命中 7/7 | ✅ 数据真实 | 但只是 atomic_facts 单源命中, 不是用户能感知 |
| `run_v22_acceptance.py` 三道门 | ✅ 工具有价值 | 测了"我新设计的东西", 没测"主仓库 75% 完成度" |
| 上量批抽脚本 | 🟡 流程对了 | 速度(11h)+成本无法接受,需要并发 |
| baseline runner P% 87% (B 写) | 🔴 atomic_facts 单源, 不查现成表 | B 自检 30% 保留度 |
| 大量 docs | ✅ 认知性高 | 部分基于错误假设(8 段),需要重写 |

**净增量价值**:
- 真正有用: document_llm_extractor + 5/19 命中数据 + acceptance runner
- 半残: baseline runner + narrative_kernel + 部分 docs
- 浪费: 跟主仓库 narrative_generator 重复实现的部分

### 4.4 B AI 5/22 资产盘点纠偏自检报告(007a8de)

B AI 已诚实承认 iterate 1 五个 commit 的偏差:
- ✅ endpoint shell 保留度 100%
- ⚠️ 测试保留度 95%
- ⚠️ 前端 hook 保留度 85%
- 🔴 **baseline runner 保留度 30%** — atomic_facts 单源
- ⚠️ MILESTONE audit 数字真实但解读偏差

详见 `docs/B_AI_ASSET_PIVOT_SELF_AUDIT.md`.

---

## 5 · 4 维度交叉评估:重大偏差清单

### 偏差 1 · 段数冲突(根因:维度 1 没读)

```
产品手册 §03:    essence / cooperation / business_intro / people / timeline / next_steps  (6 段)
主仓库实现:      narrative_generator.py 已实现 6 段
我 V2.1 设计:    identity / people / main_lines / recent_changes / risks / our_collab / open_questions / timeline  (8 段)
```

**判决**: 我 V2.1 的 8 段段名跟产品手册口径**完全不匹配**, 应该砍.

### 偏差 2 · 重复造 narrative_generator(根因:维度 2 没读)

```
主仓库 narrative_generator.py  64,497 字  (已实现 6 段叙事 + 本地 LLM 调用)
V2.1   narrative_kernel.py     13,957 字  (我重写的 8 段 deterministic)
```

**判决**: 砍 narrative_kernel.py, 改主仓库 narrative_generator.py 接现成表.

### 偏差 3 · IngestPipeline 没接通 smart_file_import(根因:维度 2 没读)

```
主仓库 smart_file_import.py:1002  直接 INSERT atomic_facts (不走 IngestPipeline)
主仓库 contradiction_detector.py:81  也直接 INSERT
```

**判决**: N1"现有功能顺畅" 真路径 = 改 smart_file_import + contradiction_detector 走 IngestPipeline.

### 偏差 4 · broadcast 钩子没接(根因:维度 2 没读)

```
产品手册 §07:   broadcast_data_changed 一处写全局刷
主仓库:         data_center_broadcast.py 已实现 L1 同步 + L2 异步 daemon + throttle
V2.1:           IngestPipeline.ingest() 写完 atomic_facts 后没调 broadcast
```

**判决**: IngestPipeline 必须在写后调 broadcast_data_changed, 否则跟产品手册 §07 设计立场冲突.

### 偏差 5 · baseline runner 只查 atomic_facts 单源(B AI 自检)

```
B AI baseline runner: FROM atomic_facts × 2 处 (line 269 + 294)
应该: 查 risk_signals (20) / open_questions (23) / weekly_reviews (9) / commitments (66)
      / meetings (7) / decisions (3) / tasks (238) / event_lines (16) / entities (4987)
      / client_dna_documents (32)
```

**判决**: baseline runner 重写, 测"现成表覆盖度"不是"atomic_facts 命中率".

### 偏差 6 · v2.2 主仓库 75% 完成度未在 V2.1 视野(根因:维度 2 没读)

主仓库自评 v2.2 Phase 2 工具层 75% 完成(`93f463c` audit), 但我在 V2.1 设计时**完全不知道**这个进度, 反复重做工具层.

**判决**: V2.1 应该**作为主仓库 v2.2 的下游补充**(LLM 抽取 / 上量等增值能力), 不是平行版本.

---

## 6 · 净评估:V2.2 真实完成度

### 主仓库视角(权威)

```
N1 现有功能顺畅           70%  (v2.0 已上线, 多数功能跑得通,
                                但 smart_file_import 等没接 IngestPipeline)
N2 数据中心 4 主路径理解   55%  (IngestPipeline 工具就位, 但 smart_file_import 没接,
                                contradiction_detector 没接, 主时间线 activity_logs 没接)
N3 3.0 接入预留           85%  (IngestPipeline + ReasoningTraceStore + IdempotencyStore
                                + key_decisions 表 + event_log 表 都有, 但 5 张 AI Memory 0 行)
```

**净完成度 ~70%**(不是我之前以为的 80-85%).

### V2.1 实验仓库的真实贡献

```
+ document_llm_extractor.py        (LLM 抽取, 主仓库可吸收)
+ 5/19 命中数据 (atomic_facts 单源)  (真实数据点)
+ 大量产品认知文档                   (北极星 / 信息商 / 5 维元数据等)
- narrative_kernel.py 8 段           (浪费, 跟主仓库 6 段冲突, 应砍)
- baseline runner 单源 P% 87%        (浪费, 应重写)
- 上量批抽脚本 11h ETA               (浪费, 应并发)
```

**净贡献评估**: +20% 增量 / -15% 重复造轮子 / 净增 +5%.

---

## 7 · 新计划(基于 4 维度评估)

### 阶段 0:**承认偏差 + 砍废**(30 分钟)

- ✅ 已 commit 资产清单 + 协作流程纠偏(5/22)
- 写本 4 维度评估文档,顾源源 review
- 砍 `narrative_kernel.py` + `test_v22_narrative_kernel.py`(标 deprecated 而不是删)
- 砍 8 段相关的 docs(8 个 段名锁定在产品手册 6 段)

### 阶段 1:**主仓库 / V2.1 收敛**(1-2 小时)

- A AI: 修改主仓库 `narrative_generator.py` 让 6 段(essence / cooperation / business_intro / people / timeline / next_steps) **接现成表数据源**(§4 映射表)
- B AI: 重写 baseline runner,测 6 段 + 现成表覆盖度
- 同步 main / v2.2-arch-v2 commits

### 阶段 2:**接通 smart_file_import + contradiction_detector**(2 小时)

- A AI: 改 `smart_file_import.py:1002` 走 IngestPipeline
- A AI: 改 `contradiction_detector.py:81` 走 IngestPipeline
- A AI: IngestPipeline.ingest() 写后调 broadcast_data_changed
- B AI: 改 SmartFileImport 前端验收(看 atomic_facts 表是否有新数据)

### 阶段 3:**dogfood + 评估**(0.5 小时)

- 顾源源在 app 打开战略陪伴, 看新 6 段叙事(命中现成表)
- 评估能不能感知到客户的真实变化

### 阶段 4:**N3 AI Memory 5 表流量**(1 小时)

- 不再加新表
- A AI: 让 IngestPipeline 顺手往 ai_episode_log 写 1 条(N3 A2)
- A AI: 让 DocumentLLMExtractor 顺手往 reasoning_traces 写 1 条(N3 A3)
- 跑一遍, 看 5 表是否有数据

### 阶段 5:**砍掉的事**

- ❌ 上量跑 62 份(11 小时浪费,只有 3/62 完成)
- ❌ v1 LLM 编排(基于错段数草稿, 6 段重新设计后再说)
- ❌ event_line_activities 灌 atomic_facts(直接读 SELECT 不灌)
- ❌ 改更多 view 接旧 8 段 endpoint(B AI iterate 1 已 1/3 完成的)

---

## 8 · 强制流程(顾源源 5/22 钦定,加入协作文档)

```
每个里程碑结束 / "重新评估" / "新计划" 之前, 强制按 4 维度盘点:

维度 1 · 软件功能架构
  cat docs/V2.2_PRODUCT_MANUAL_FULL_TEXT.md
  (9 份产品手册的精炼)

维度 2 · 主仓库代码结构
  ls ~/openclaw/workspace/yiyu-thinktank-workbench/backend/app/services/ | wc -l
  cd ~/openclaw/workspace/yiyu-thinktank-workbench && git log --since="24h" --pretty=format:"%h %s" | head
  (看主仓库今天动了什么, 我跟它在不在一起)

维度 3 · 数据库表
  cat docs/V2.2_DATA_ASSET_INVENTORY.md
  (208 张表清单)

维度 4 · V2.1 实验架构
  cd ~/openclaw/workspace/V2.1 && git log --since="24h" --pretty=format:"%h %s"
  comm -23 <(ls backend/app/services/ | sort) <(ls ~/openclaw/workspace/yiyu-thinktank-workbench/backend/app/services/ | sort)
  (我真新增了什么 vs 主仓库)

→ 4 维度全跑完, 出"偏差度评估"
→ 写新计划(不是直接执行)
→ 顾源源 review 计划
→ 才执行
```

**这个流程加入 `docs/V2.1_AI_COLLABORATION.md` §7**(里程碑结束必做).

---

## 9 · 我做出来的承诺

1. ✅ 把 9 份产品手册存到长期共享记忆(`docs/V2.2_PRODUCT_MANUAL_FULL_TEXT.md`)
2. ✅ 跑完 4 维度盘点
3. ✅ 写本评估报告
4. ⏭️ 把 4 维度流程加到协作文档 §7
5. ⏭️ 等顾源源 review 本评估 → 给阶段 1 起步令

---

**A AI · 2026-05-22 · 4 维度全盘评估完毕**

# V2.2 新计划 · 2026-05-22(基于 4 维度评估 + 主仓库现状)

> **前置阅读**: `docs/V2.2_ASSESSMENT_4D_20260522.md`(4 维度评估报告)
> **核心发现**: 主仓库 `narrative_generator.py` (1156 行) + `narrative_collector.py` 已实现产品手册 §03 钦定的 6 段叙事 + 现成表拉取。V2.1 NarrativeKernel 8 段是 100% 重复造轮子。
> **新计划方向**: V2.1 作为主仓库的下游补充, 不是平行版本。砍假增量, 修接通点。

---

## 0 · 主仓库现状(决定计划的真实起点)

### 0.1 主仓库 narrative_generator.py 已实现

```python
DIMENSIONS: tuple[str, ...] = (
    "essence",          # Layer 1 (组织介绍)
    "cooperation",      # Layer 2 (合作关系)
    "business_intro",   # Layer 3 (业务介绍)
    "people",           # Layer 4 (关键人物)
    "timeline",         # Layer 5 (时间线)
    "next_steps",       # Layer 6 (本阶段战略思路)
)
```

跟产品手册 §03 6 段叙事**字段名 100% 匹配**。

### 0.2 主仓库 narrative_collector.py 已拉的现成表

| 拉的表 | 行数 | 用于哪段 |
|---|---|---|
| `clients` | 12 | essence |
| `client_glossary` | 407 | 跨段(术语) |
| `glossary_relations` | 63 | 跨段(关系) |
| `glossary_attributes` | 808 | 跨段(属性) |
| `risk_signals` WHERE status='active' | 20 | next_steps |
| `tasks` | 238 | cooperation / next_steps |
| `weekly_review_task_entries` | 17 | cooperation |
| `meetings` | 7 | timeline |
| `module_definition_entries` | 133 | business_intro |
| `v2_chunks` + `v2_documents` + `documents` | 11998 + 998 + 1922 | 跨段(原文片段) |

### 0.3 主仓库 collector 漏拉的关键表(N2 缺口)

| 漏的表 | 行数 | 应拉到哪段 |
|---|---|---|
| `entities` WHERE type='person' | (从 4987 筛) | people |
| `entity_mentions` | 12184 | people |
| `event_lines` | 16 | cooperation / next_steps |
| `event_line_activities` | 104 | timeline |
| `activity_logs` | **26677** | timeline ★ 主时间线 |
| `commitments` WHERE status='open' | 66 | next_steps |
| `decisions` | 3 | timeline |
| `client_dna_documents` | 32 | essence |
| `organization_dna_v2_items` | 199 | essence |
| `client_strategic_profiles` | 6 | essence / business_intro |
| `event_line_memory_snapshots` | 17 | timeline |
| `dna_deltas` | 13 | timeline(变更线) |

### 0.4 关键集成链路(产品手册 §07 + 主仓库现状)

```
用户行为                           主仓库已实现?
─────────────────────────────────────────────
智能文件导入 § 06                  smart_file_import.py ✅ 已实现, 但 INSERT atomic_facts 不走 IngestPipeline ❌
     ↓
IngestPipeline.ingest()           ✅ 已实现 (e225e9b), 但没被 smart_file_import 调用
     ↓
broadcast_data_changed()          data_center_broadcast.py ✅ 已实现 (L1 同步 + L2 异步)
     ↓
narrative regenerate (6 段)        narrative_generator.py ✅ 已实现 + collector ✅ 已实现 (但漏拉 8 张关键表)
     ↓
战略陪伴 § 03 主面板               前端 ✅ 已实现 (NarrativePanel 6 段)
     ↓
顾源源 / 同事在 app 里看到         ?(待 dogfood)
```

**真正的 N1+N2 缺口** = **3 个接通点**:
1. smart_file_import 改走 IngestPipeline
2. IngestPipeline.ingest() 写后调 broadcast_data_changed
3. collector 补 8 张关键漏拉表

不是"从头重写 NarrativeKernel"。

---

## 1 · 新计划 6 阶段(总时长 6-8 小时)

```
阶段 0 · 砍假增量(30 min)
  ↓
阶段 1 · dogfood 主仓库 6 段现状(45 min)
  ↓
阶段 2 · 补 collector 漏拉的 8 张表(2 h)
  ↓
阶段 3 · 接通 smart_file_import → IngestPipeline → broadcast(2 h)
  ↓
阶段 4 · N3 AI Memory 5 表真流量(1 h)
  ↓
阶段 5 · 全流程验收 + 顾源源最终 dogfood(45 min)
```

---

### 阶段 0 · 砍假增量(30 分钟)

**目的**: 把 V2.1 跟产品手册 / 主仓库冲突的代码 + 文档标 deprecated,不再混淆视野。

**A AI(我)做**:
1. 把 `backend/app/services/narrative_kernel.py` 重命名为 `narrative_kernel.py.DEPRECATED`(保留历史,不删)
2. 把 `backend/tests/test_v22_narrative_kernel.py` 同样重命名为 `.DEPRECATED`(保留测试历史)
3. 在文件头加废弃声明:
   ```python
   """[DEPRECATED 2026-05-22] 这是 V2.1 实验仓库自写的 8 段 NarrativeKernel.
   产品手册 §03 钦定 6 段叙事 (essence/cooperation/business_intro/people/timeline/next_steps),
   主仓库 narrative_generator.py 1156 行已完整实现 6 段 + 现成表 collector.
   砍废理由 + 替代方案见 docs/V2.2_NEW_PLAN_20260522.md
   """
   ```
4. `docs/V2.2_NARRATIVE_KERNEL_V1_PROMPT_DRAFT.md` 同样标 DEPRECATED
5. `docs/V2.2_F21_AI_INFORMATION_NEEDS.md`(9 层抽取)同样标(超出产品手册口径)
6. commit `[A] chore(v2.2 ★ pivot): 砍 V2.1 NarrativeKernel 8 段 — 跟产品手册 6 段冲突`

**B AI 做**:
1. 把 `scripts/run_v22_n2_baseline.py` (baseline runner)标 DEPRECATED(B AI 自检承认 30% 保留度)
2. 让前端 `useClientFullNarrative` hook 不再用,改用主仓库已有的 narrative_generator endpoint(待 A 阶段 1 确认 endpoint URL)
3. commit `[B] chore(v2.2 ★ pivot): 砍 V2.1 baseline runner + frontend hook 旧版`

**验收门**:
- V2.1 `git grep -l "narrative_kernel\|NarrativeKernel\|8 段"` 结果 ≤ 5 个文件(都是文档,不是代码)
- 主仓库 v2.2-arch-v2 分支无破坏

**风险**: 重命名后如果 v2.2 acceptance script 调用了 narrative_kernel,会报错。**对策**:同步把 `scripts/run_v22_acceptance.py` 里 NarrativeKernel 相关检查移除。

---

### 阶段 1 · dogfood 主仓库 6 段现状(45 分钟)

**目的**: 顾源源在 app 里看一眼主仓库的 6 段叙事现在长什么样,定基线。不是猜,是看。

**A AI 做**:
1. 用日慈基金会客户在本地 prod db 上跑一次 `narrative_generator.generate(client_id='日慈基金会')`
2. dump 输出到 `docs/dogfood_narrative_6dim_baseline_20260522.md`
3. 列出 6 段每段:
   - LLM 真实输出文本
   - 把握度(high/medium/low)
   - 引用源(从哪些表拉的)
4. 标注哪些段"看着完整"vs"看着空/浅"(这就是 collector 漏拉表带来的缺口)

**顾源源做**:
1. 用 V2.1 build 本地 dev app 或者直接看主仓库 `running` 状态的 app
2. 切到战略陪伴 → 选日慈基金会 → 看 6 段叙事
3. 给反馈(15-30 分钟):
   - 哪段够好,哪段空
   - 5/19 张真会议关键事实(7 道金标准)能不能在 timeline / next_steps / people 段中看到
   - 「让 AI 重新理解」按钮触发后,内容是否真的更新

**B AI 做**:
1. 看 `narrative_generator.py` 的 endpoint URL(可能是 `/clients/:id/narrative` 或类似)
2. 确认前端 `NarrativePanel` 是不是已经接通这个 endpoint
3. 报告:**用户已经能在 app 里看到 6 段叙事吗?**

**验收门**:
- 顾源源给定 6 段 baseline 评分(每段 high/med/low)
- 输出 markdown 在 `docs/dogfood_narrative_6dim_baseline_20260522.md`

**风险**: 主仓库的 narrative_generator 可能调用本地 AiService 失败(豆包 token 配置/网络/local model)。**对策**:先验证 `client_strategic_pulse` 等其他调 LLM 服务能跑,确认 LLM 配置 OK。

---

### 阶段 2 · 补 collector 漏拉的 8 张表(2 小时)

**目的**: 让主仓库 narrative_collector.py 拉到 §0.3 表列的 8 张关键表,让 6 段叙事的"原料"完整。

**A AI 做**(主仓库 + V2.1 同步):

1. 改 `backend/app/services/narrative_collector.py`,补 8 张表的 SELECT:

   ```python
   # 新加方法
   def _collect_people_entities(self, client_id: str) -> list[PersonFact]:
       # FROM entities WHERE client_id=? AND entity_type='person'
       # ORDER BY mention_count DESC LIMIT 30
       ...

   def _collect_event_lines(self, client_id: str) -> list[EventLineFact]:
       # FROM event_lines WHERE primary_client_id=?
       # cooperation 段 + next_steps 段共用
       ...

   def _collect_main_timeline(self, client_id: str) -> list[TimelineEntry]:
       # FROM activity_logs WHERE entity_type='client' AND entity_id=?
       # UNION FROM event_line_activities WHERE event_line_id IN (...)
       # ORDER BY happened_at DESC LIMIT 50
       ...

   def _collect_open_commitments(self, client_id: str) -> list[CommitmentFact]:
       # FROM commitments WHERE client_id=? AND status='open'
       ...

   def _collect_client_dna(self, client_id: str) -> list[DnaDocument]:
       # FROM client_dna_documents WHERE client_id=?
       # essence 段直接读
       ...

   def _collect_strategic_profile(self, client_id: str) -> StrategicProfile:
       # FROM client_strategic_profiles WHERE client_id=?
       ...

   def _collect_dna_deltas(self, client_id: str) -> list[DnaDelta]:
       # FROM dna_deltas WHERE client_id=? ORDER BY created_at DESC LIMIT 10
       ...

   def _collect_decisions(self, client_id: str) -> list[DecisionFact]:
       # FROM decisions JOIN meetings WHERE meetings.client_id=?
       ...
   ```

2. 改 `narrative_generator.py` 让 6 段 prompt 引用新的 fact 集合:
   - **essence**: 加 client_dna_documents + client_strategic_profile
   - **cooperation**: 加 event_lines(主线)
   - **business_intro**: 加 client_strategic_profile.industry/scale
   - **people**: ★ 用 entities (type='person') 不是只看 atomic_facts
   - **timeline**: ★ 用 activity_logs 作主线 + event_line_activities 补充
   - **next_steps**: 加 open commitments + event_lines.next_step

3. 写单测 `test_narrative_collector_additions.py`(20 个测试)

**B AI 做**:
1. 改前端 `NarrativePanel` 的 hook,展示新增字段(如果有的话)
2. 写 endpoint 集成测试

**验收门**:
- 单测 PASS
- 阶段 1 baseline 评分至少 3 段从 low → medium
- collector 调用的表覆盖 §0.2 + §0.3 全部 21 张表
- 5/19 张真会议金标准 7 道命中 ≥ 5 道(在 6 段叙事中)

**风险**:
- collector SQL 失败 → 单测捕获
- LLM prompt 加新 fact 后 token 超限 → 加 truncate

---

### 阶段 3 · 接通 smart_file_import → IngestPipeline → broadcast(2 小时)

**目的**: 修补 N1 真路径 — 现有功能顺畅的核心断点。

**A AI 做**(主仓库):

1. 改 `backend/app/services/smart_file_import.py:1002`
   ```python
   # BEFORE:
   db.execute("""INSERT INTO atomic_facts ...""", params)
   
   # AFTER:
   from app.services.ingest_pipeline import IngestPipeline, IngestRequest, metadata_for_workbench_file
   pipeline = IngestPipeline(db)
   req = IngestRequest(
       path="workbench_file",
       client_id=client_id,
       subject_text=...,
       attribute=...,
       value_text=...,
       metadata=metadata_for_workbench_file(...),
       source_v2_document_id=...,
       evidence_text=...,
   )
   result = pipeline.ingest(req)
   ```

2. 改 `backend/app/services/contradiction_detector.py:81` 同样改走 IngestPipeline

3. 改 `backend/app/services/ingest_pipeline.py` 的 `IngestPipeline.ingest()` 在写完 atomic_facts 后调 broadcast:
   ```python
   from app.services.data_center_broadcast import broadcast_data_changed
   
   # 在 ingest() 末尾 (写完 atomic_facts + event_log + ai_episode_log 之后)
   broadcast_data_changed(
       client_id=req.client_id,
       reason=f"ingest_pipeline:{req.path}",
       fact_ids=[result.fact_id] if result.written else [],
   )
   ```

4. 集成测试:跑 smart_file_import 一次,验证:
   - atomic_facts 表新增
   - broadcast 触发 analysis_job 入队
   - narrative_stale_signals 标记
   - 后台 daemon 跑 narrative regenerate

**B AI 做**:
1. 前端 smart_file_import 模态关闭后,验证 5 秒内能看到工作台文件数 +N
2. 1-2 分钟后验证战略陪伴 6 段叙事自动刷新

**验收门**:
- smart_file_import 完成 → atomic_facts 增量 ≥ 50 条
- broadcast 触发 → narrative_stale_signals 标记
- 60-90 秒后 narrative_generator 自动跑 → 6 段叙事更新

**风险**:
- IngestPipeline 跟 smart_file_import 的事务边界冲突(后者已有 BEGIN IMMEDIATE)→ 用嵌套保存点 或者 把 IngestPipeline 调用挪出原事务
- broadcast 触发过频 → 用 throttle(主仓库已有 60-90 秒 throttle 机制)

---

### 阶段 4 · N3 AI Memory 5 表真流量(1 小时)

**目的**: 让 reasoning_traces / ai_episode_log / ai_feedback_signals / ai_improvement_suggestions / ai_learned_rules 5 表有真数据,不再 0 行。

**A AI 做**:
1. IngestPipeline.ingest() 顺手往 ai_episode_log 写 1 条(N3 A2 已设计但没接通):
   ```python
   if req.ai_session_id and req.metadata.actor_type == "ai_agent":
       log_ai_episode(
           db, ai_session_id=req.ai_session_id, client_id=req.client_id,
           action_type=f"ingest_{req.path}",
           referenced_fact_ids=[result.fact_id],
           ...
       )
   ```
2. DocumentLLMExtractor 顺手往 reasoning_traces 写 1 条/batch(N3 A3 已设计但没接通)
3. smart_file_import 走 IngestPipeline 后,actor_type 设 "human"(用户主动导入), ai_episode_log 不写; 但 reasoning_traces 写(LLM 抽实体的过程)

**验收门**:
- 跑一次 smart_file_import: ai_episode_log ≥ 0(human 不写), reasoning_traces ≥ 1
- 跑一次 DocumentLLMExtractor(LLM 抽 docx): ai_episode_log ≥ 5, reasoning_traces ≥ 1
- 总体:5 张 AI Memory 表至少 3 张有数据

**风险**: ai_episode_log schema 可能跟当前 IngestRequest 字段不全匹配 → 加 adapter 函数。

---

### 阶段 5 · 全流程验收 + 顾源源最终 dogfood(45 分钟)

**A AI 做**:
1. 跑 v22 acceptance 三道门(主仓库 + V2.1 一起)
2. 跑 dogfood 演示脚本:
   - 用日慈基金会跑一次完整流程:smart_file_import → 6 段叙事更新 → activity_logs 新增
3. 输出 `docs/MILESTONE_V2.2_NEW_PLAN_COMPLETE_AUDIT.md`

**顾源源做**:
1. 打开 app(本地 dev)
2. 切到日慈基金会工作台 → 智能文件导入 → 拖 2 份新 docx → 写一段背景 → 提交
3. 等 60-90 秒
4. 切到战略陪伴 → 看 6 段叙事是否自动刷新
5. 切到任务与日程 → 看 timeline 段在不在新内容里出现
6. 给最终判决:**V2.2 是否可以宣告完成**

**验收门(V2.2 完成定义)**:
| 验证项 | 通过标准 |
|---|---|
| 看得见 | 6 段叙事在战略陪伴里展示,5/19 张真会议 7 道金标准 ≥ 5 道 |
| 答得出 | 工作台 AI 对话:"日慈最近什么变化",AI 答出 ≥ 5 条 |
| 不掉链 | acceptance 三道门 PASS + 智能文件导入 → 6 段自动刷新 |
| AI Memory | 3 张以上 ai_*/reasoning_* 表有数据 |

---

## 2 · 取舍 — 砍掉什么(不做的事)

| 砍掉 | 理由 |
|---|---|
| ❌ 上量跑 62 份 docx(11h 浪费) | 主仓库 narrative_collector 不需要 LLM 抽全量, 直接 SELECT 现成表 |
| ❌ NarrativeKernel v1 LLM 编排 | 主仓库 narrative_generator 已有 LLM 编排 |
| ❌ event_line_activities 灌 atomic_facts | collector 直接 SELECT, 不灌 |
| ❌ 8 段叙事相关测试 / endpoint / 前端 hook | 全砍, 用主仓库 6 段 |
| ❌ 新建 schema / 表 | N3 已 90%, 工程冻结 |
| ❌ baseline runner 单源 P% | B AI 已自检, 重写为现成表覆盖度测试 |
| ❌ data_center_ingest_events 自检环 | 主仓库已有 broadcast, 不需要 |
| ❌ key_decisions / org_events 表新写入路径 | 已建,等 commitment / decision 流量自然进 |

---

## 3 · A AI / B AI 分工(纠偏后)

### A AI(我)主导:
- 阶段 0 砍 narrative_kernel + 相关 docs
- 阶段 1 dogfood 6 段现状(脚本)
- 阶段 2 collector 补 8 张表 + LLM prompt 改 + 单测
- 阶段 3 smart_file_import + contradiction_detector 改走 IngestPipeline + broadcast 接通
- 阶段 4 IngestPipeline 写 ai_episode_log / DocumentLLMExtractor 写 reasoning_traces
- 阶段 5 audit 文档

### B AI 主导:
- 阶段 0 砍 baseline runner + frontend hook 旧版
- 阶段 1 前端 NarrativePanel 是否真接通 endpoint 报告
- 阶段 2 endpoint 集成测试 + 前端 6 段渲染
- 阶段 3 smart_file_import 前端流程验证
- 阶段 5 9 模块手动过

### 共享文件(改前 read):
- `docs/V2.2_PROGRESS.md`
- `docs/V2.2_NEW_PLAN_20260522.md`(本文档)
- `backend/app/services/ingest_pipeline.py`(A 主改, B 看)
- `backend/app/services/narrative_collector.py`(A 主改, B 看)
- `backend/app/services/narrative_generator.py`(A 主改, B 看)
- `backend/app/services/smart_file_import.py`(A 主改, B 看)

---

## 4 · 时间表 + 顾源源参与点

```
T+0     A: 阶段 0 砍废开干
T+30m   A: 阶段 0 完成 commit
        B: 阶段 0 完成 commit
T+30m   A: 阶段 1 dogfood 脚本跑
        B: 阶段 1 前端报告
T+1h15m 顾源源 review baseline (~15 min)
        --- 决策点 1: 阶段 2 起步令 ---
T+1h30m A+B: 阶段 2 collector 补表
T+3h30m 阶段 2 完成 + 5/19 ≥ 5/7 命中
        --- 决策点 2: 阶段 3 起步令 ---
T+3h30m A+B: 阶段 3 smart_file_import 接通
T+5h30m 阶段 3 完成 + smart_file_import → 6 段自动刷新
        --- 决策点 3: 阶段 4 起步令 ---
T+5h30m A: 阶段 4 AI Memory 流量
T+6h30m 阶段 4 完成
T+6h30m A+B: 阶段 5 全流程验收
T+7h15m 顾源源 final dogfood (~30 min)
        --- V2.2 完成判决 ---
T+7h45m audit 文档完成
```

**决策点 1-3 顾源源参与 ~15 分钟/次** + **决策点 4 顾源源 dogfood ~30 分钟**。

---

## 5 · 风险清单

| 风险 | 影响 | 兜底 |
|---|---|---|
| 主仓库 narrative_generator LLM 调用失败 | 阶段 1 dogfood 看不到内容 | 先验证 LLM 配置, 再跑 |
| collector 加新 SQL 字段冲突 | 阶段 2 单测失败 | 加 LEFT JOIN 兜底, 字段不存在不抛错 |
| smart_file_import 事务嵌套 | 阶段 3 数据丢 | 用 SAVEPOINT 或者 IngestPipeline 调用挪出原事务 |
| broadcast throttle 触发 | 阶段 3 narrative 不刷新 | 看 data_center_broadcast 的 throttle 时间窗口, 给手动 force 入口 |
| LLM 6 段输出超 token | 阶段 2 LLM 失败 | 每段独立调 LLM(主仓库已设计) |
| 顾源源 dogfood 后说"还不是故事" | 阶段 5 验收失败 | 6 段 prompt 调优 / 加 v0.2 设计点 |

---

## 6 · V2.2 完成后的下一步(预告)

阶段 5 完成 = V2.2 工具层 + 接通层 + 客户感知 三件套齐:

```
V2.2 完成 (工具层 + 接通 + 用户感知)
   ↓
V2.3 (优化期, 不在本计划范围):
   - 上量批抽并发优化 (P1+P3, 客户体验)
   - smart_file_import 异步队列 (用户不等待)
   - 6 段 LLM prompt 精细化
   - dogfood feedback driven 改进
   ↓
V3.0 (智能公司, 不在本计划范围):
   - AI Memory 5 表读取逻辑 (现在只写)
   - 多 AI 角色协作 (撰稿员 / 数据员 / 法律员)
   - AI 团队订阅事件 (取代 broadcast 一处刷全)
```

---

**A AI · 2026-05-22 · 新计划完整版**
**等顾源源 review 后 → 阶段 0 起步令**

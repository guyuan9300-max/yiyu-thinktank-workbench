# Week 2 验收方案 · 模块化骨架 + LLM Context 一等公民

> Week 2 跟 Week 1 一样**没有 UI 改动**,验收只走"客观自动化"路径。
> 本方案覆盖 **10 个维度**,跑法:`python3 scripts/run_w2_acceptance.py`
> 通过 = 所有维度 PASS

---

## Week 2 三大产出

| # | 产出 | 文件 |
|---|---|---|
| W2-A | 模块 boundary linter | `scripts/lint_module_boundaries.py` + `tests/test_lint_module_boundaries.py` |
| W2-B | LLM Context 层雏形 | `backend/app/llm_context/{__init__,types,schema,composer,prompt_logger,inspector}.py` + 测试 |
| W2-C | 7 业务模块骨架 | `backend/app/modules/{client,commitment,glossary,intelligence,knowledge,narrative,task}/` |

完整模块:`commitment`(单表最简) + `glossary`(多表,有 services 待迁移)
占位模块:`client`(W3) + `task`(W3-4) + `knowledge`(W4) + `intelligence`(W3-4) + `narrative`(W5)

---

## 验收门(10 维度)

| # | 维度 | 类型 | 期望 |
|---|---|---|---|
| 1 | 模块 boundary linter 自检 | pytest | 9 tests pass |
| 2 | 模块 boundary 实跑(全 codebase) | shell | exit 0,8 个模块 0 violation |
| 3 | LLM Context schema + 3 件套 | pytest | 21 tests pass |
| 4 | 7 业务模块 import + 接口 | pytest | 29 tests pass |
| 5 | 完整 2 模块(commitment + glossary)方法正确性 | pytest | 含在维度 4 里 |
| 6 | 占位 5 模块走 SQL view(SSOT 通路) | pytest | 含在维度 4 里 |
| 7 | W1 全套(58 tests + 真实 e2e + golden diff)仍通过 | runner | 12/12 PASS(reuse W1 runner) |
| 8 | 数据真实性 + 启动健康 + 性能 | runner | reuse W1 runner |
| 9 | 新建的 prompt_log 表 + 索引存在 | pytest | 含在 W2-B 测试里 |
| 10 | 模块自包含(没有从其他模块的 internal 文件 import) | shell | 维度 2 的子集 |

---

## 累计统计

W1 + W2 累计:
- **122 个 pytest cases** 全过
- **2 个 lint 跑通 0 violation**
- **golden diff 100% PASS**(v2.1 全程不动 v1.0 数据)
- **8 个模块** 通过 boundary 检查
- **新增 1 张表**(prompt_log)+ 0 张表 schema 改动

---

## 失败处理

任一维度 FAIL → 不能进 Week 3。
